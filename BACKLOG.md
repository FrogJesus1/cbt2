# Engine Backlog

A running log of bugs, data gaps, and improvements across all engines in this system.
The organizing principle is **determinism**: every item notes what it would unlock in terms
of the engine being able to answer a question with a computed result instead of a stub.

**Determinism levels (for tracking progress):**
- `D0` — command exists, returns stub or None for the core value
- `D1` — command returns real data from loaded files, no computation
- `D2` — command returns computed results (probability, score, ranking)
- `D3` — command can answer follow-up/compound questions without extra prompting

---

## How to Use This File

- Read this file at the start of any dev session involving this project
- When you discover a bug, gap, or improvement during a session → add it here immediately
- When you fix something → mark it `[FIXED YYYY-MM-DD]` and note what changed
- When adding a new engine → create a matching section for it
- The goal is not a perfect backlog — it's a honest snapshot of where each engine stands

---

## Engine: `combat_terminal`

### Bugs

| Status | Priority | Description | Component | Fixed In |
|--------|----------|-------------|-----------|----------|
| 🟠 PARTIAL | **HIGH** | **Login shows no profiles — Airtable read hangs on prod** — reported 2026-06-19, right after Phase 7. Diagnosis from the live API: `GET /api/version` is instant but `GET /api/profiles` **and** `/api/profiles/health` hang (180s timeout) → FastAPI is fine; the outbound Airtable call to the `Profiles` table can't complete (it *hangs*, so it's not a missing token — that errors instantly — but network egress to api.airtable.com from Phil-2 / DNS / a retry-storm). The login then showed nothing because the fetch never returned and the UI swallowed it. **Code fix (shipped, both layers honest now):** (a) frontend `ProfileGate.loadProfiles` races `fetchProfiles` against a **9s timeout** and renders a real `⚠ Can't reach the profile store` view with `↻ retry` + `+ new profile` + `skip →` instead of an infinite "Scanning…" or a blank picker; (b) backend `profiles._get_table` now passes `Api(token, timeout=(5,20))` (defensive `TypeError` fallback) so every Airtable call **fails fast → 502 with the real reason** instead of hanging the request. esbuild graph clean; **172 py tests green** (incl. the 13 API degrade-gracefully tests; pyairtable 2.3.7 confirmed to accept the `timeout` kwarg). **Still needs (infra, not code):** on Phil-2, confirm `AIRTABLE_TOKEN`/`AIRTABLE_BASE_ID` (appfOqXB5mLMgXFKp) are set in the *running* container, the token isn't expired, the `Profiles` table exists in that base, and the host can actually reach `https://api.airtable.com` (egress/DNS). Once deployed, `curl -s https://war.juzzie.xyz/api/profiles/health` should now return JSON in ≤20s (`{ok,count,error}`) instead of hanging — that pinpoints the cause. | `render/web/src/components/ProfileGate.jsx`, `render/web/profiles.py` (code); Phil-2 Airtable env (infra) | 2026-06-19 (code) |
| ✅ DONE | FEAT | **Login + boot + Error/Loading motion (UI redesign Phase 7)** — final redesign phase; all phases now landed (holistic visual pass remaining). *Foundation:* `index.css` Phase-7 motion vocabulary, all `prefers-reduced-motion`-gated — `.ct-crt-scanlines`/`.ct-crt-vignette`, `.ct-flicker`/`.ct-power-on`/`.ct-boot-scan` (boot), `.ct-ctx-sweep` (context switch), `.ct-sim-track` (indeterminate "simulating" bar), `.ct-reveal`, `.ct-pulse`; new `hooks/useCountUp.js` (`useCountUp` + `<CountUp>`, easeOut, reduced-motion→snap, null→dash). *Login:* `ProfileGate.jsx` **fully rewritten** to `Login.dc.html` — CRT cogitator-boot console (banner + live `useEdition` chip + streamed boot log → `▸ READY`, once/session via `sessionStorage ct_booted`) → pick/PIN/create/skip (`PickView`/`PinView`/`CreateView`, gold=PIN·green=open dot, `🔒 PIN`, custom checkbox, footer caret); dropped hardcoded `#39ff14`/JetBrains for `--ct-*` tokens (`data-theme` from `ct_active_theme`; boot-console darks fixed). All profile APIs + auto-login preserved. *Errors:* full-screen `⚠ COGITATOR UNREACHABLE` takeover (python-main.py hint, reconnect/offline-mode, 5s auto-retry re-probing `/api/engines`) replaces the thin red banner; amber "Monte Carlo offline · deterministic mode" degraded banner; tri-state chrome dot (online/degraded/no-link) + `DEGRADED`/`NO LINK` chip; new `loadEngines` cb + `/status` snapshot refetched via `healthNonce`. `TerminalError` restyled (red left-accent `✕ command not recognised`, "Did you mean" bordered cards w/ `↵ N`, `help` line + caret). *Motion:* `CommandBar` `busy`-gated `⟳ simulating · N iterations` strip (N from live trials; hidden when offline/disabled); `.ct-reveal` on each `Terminal` stream entry; `key={activeContext}` `.ct-ctx-sweep` on nav change; `HeroTiles` four hero numbers now `<CountUp>`. No router/new stores; esbuild parse + App.jsx & ProfileGate.jsx graph bundles clean; `index.css` parses clean; **159 py tests green** (no `.py` touched). | `render/web/src/index.css`; `render/web/src/components/ProfileGate.jsx`, `App.jsx`, `TerminalBlock.jsx`, `Terminal.jsx`, `combat/HeroTiles.jsx`; `render/web/src/hooks/useCountUp.js` | 2026-06-19 |
| 🔵 OPEN | LOW | **Phase-7 motion not yet visually verified** — esbuild/tests are green but the sandbox can't run `vite build` or a browser, so the boot/CRT/reveal/flicker timings, the connection-lost takeover, and the degraded banner haven't been eyeballed live. Fold into the holistic visual pass: check default + ≥1 faction theme + the White Scars light theme (the login boot-console stays dark intentionally), and confirm reduced-motion fully stills the screen. | `render/web/src/index.css`, `ProfileGate.jsx`, `App.jsx` | — |
| 🔵 OPEN | LOW | **Count-up wired only into combat HeroTiles (Phase 7)** — `<CountUp>` reveal-motion is applied to the four combat hero numbers as the representative surface; other hero-number candidates (Spec combat-ratings, threat metrics, total-output panels) still snap. Extend in the visual pass if it reads well. Also: the login boot-log counts (`29 factions · 1,284 units`) are flavour text, not live — wire to `/api/factions` if desired. | `render/web/src/hooks/useCountUp.js` consumers; `ProfileGate.jsx` | — |
| ✅ DONE | FEAT | **Settings + Rules (UI redesign Phase 6)** — *Decisions:* E2 (Math Mode = display preference, not a global engine mutator — `set_mc_trials`/`get_mc_config` are global module state shared across sessions, feeding the load-bearing math, with no API setter), F1 (real `ct_aliases`), G1 (`/api/rules` + `/api/keywords`). *Server:* two new read-only endpoints serving the engine's own `rules.json` (108) + `keyword_dictionary.json` (61), loaded-once + cached, graceful empty fallback — single source of truth. *New `lib/aliases.js`* (read/write/expand, dedupe-by-alias). *New `SettingsPage.jsx`* — 4 sub-tabs replacing the hidden settings terminal: **Diagnostics** embeds `DiagnosticsPage` (standalone `diag` context **removed**, folded in — no double-mount); **Command List** LIVE from `/api/engines/{id}/schema` grouped by command group; **Math Mode** (E2) edition (real, `useEdition`) + mode/iters/confidence/toggles seeded from the engine's live status, persisted to `ct_math_prefs`; **Aliases** (F1) `ct_aliases` CRUD. *Rewrote `RulesContext.jsx`* into the rules browser — merges+dedupes the two endpoints into 123 categorised entries (56/61 keywords merge with their rule; `math_relevant` → ✓/~ SIM badge), category rail w/ live counts + dynamic Source filter + sim-only toggle + expandable cards; no router, `rule_block`/`TerminalRuleBlock` left intact. *App.jsx:* `settingsTab` state, dropdown items set context+tab directly, alias expansion + `alias <s> <e>` creation + `diag`/`commands`/`aliases`/`rule <term>` routing in `handleGlobalCommand`, `NON_TERMINAL`={rosters,crusade,settings,rules}, removed dead settings-Terminal/`openInMain`/`SETTINGS_BOOT_LINES`/diag-panel. esbuild graph clean; **159 core + 13 API tests green**; both endpoints smoke-tested via the real app. | `render/web/server.py`; `render/web/src/components/SettingsPage.jsx`, `RulesContext.jsx`, `App.jsx`; `render/web/src/lib/aliases.js` | 2026-06-19 |
| 🔵 OPEN | LOW | **Rules browser faction filter → Source filter (real-data substitution)** — Phase 6. The mockup's faction dropdown can't be populated: the real rules dataset is core-rules-centric, not faction-scoped. Shipped a dynamic **Source** filter instead (Core / Faction / Pariah Nexus / Chapter Approved), shown only when the current view spans ≥2 sources. If faction-tagged rules data lands later, swap in a faction filter. | `render/web/src/components/RulesContext.jsx` | — |
| 🔵 OPEN | LOW | **Alias profile-sync deferred (F1)** — user aliases (`ct_aliases`) are localStorage-only; the F1 spec mentioned profile-sync. Wire into `collectCurrentState`/`saveProfileState` when convenient so aliases follow a profile across devices. | `render/web/src/lib/aliases.js`, `render/web/src/lib/profile.js` | — |
| ✅ DONE | FEAT | **Rosters + Upload + Crusade restyle (UI redesign Phase 5)** — *Rosters:* hazard `SectionHeader`s, `auto-fit` ACTIVE ARMIES panel grid, accent-on-load `ArmyPanel` border/left-bar + Chakra army name, `factionColor()` dots on shared-roster rows; the "UNDER CONSTRUCTION — BUGS LIKELY" banner reframed to a calm "◉ ROSTER-SCOPED COMBAT" info note (kept the roster-restriction explanation). *Upload:* PASTE→DETAILS→LOAD→DONE step rail + a Details unit-review editor (per-unit nickname + leader→bodyguard attach via shared `parseRosterUnits`, persisted through the **existing** `/api/rosters/save-metadata` `# @nickname:`/`# @attach:` format — no new store) + a DONE confirmation; parent contract split `onLoad`/`onComplete`. *Crusade (Decision D2):* `CrusadeCard` gained a read-only→`✎ Edit`→`✓ Save` gate with manual **steppers** for BattlesFought/Survived/EnemyKills/CrusadePoints + a `✝ Mark died` toggle (rank stays XP-derived); rank-colored OOB rows + `✝ SLAIN` dim/strikethrough; rank display `Fresh`→"Battle-ready" + Legendary purple `#a78bdb` (`rankColor`/`rankLabel`/`RANK_COLORS` in `crusade/ui.jsx`); campaign detail 2-col (`minmax(0,1fr) 320px`) + Crusade Pts header stat. *Store:* `Died` checkbox added to `TABLE_SPECS`/`_UNIT_FIELD_MAP`/`_record_to_unit`; `update_unit` preserves a hand-written `crusade_points` (skips honour−scar auto-derive) and defensively drops `Died`+retries on a pre-column base. Generic `PUT /units/{id}` carries `died` (no bespoke endpoint). esbuild graph clean; **191 py tests green**. | `render/web/crusade_store.py`; `render/web/src/components/RostersContext.jsx`, `CrusadeContext.jsx`, `crusade/{ui,OrderOfBattle,CrusadeCard}.jsx` | 2026-06-19 |
| 🔵 OPEN | LOW | **Crusade `Died` column needs live-Airtable confirmation** — Phase 5 wired the `Died` (checkbox) field to the documented name with a defensive drop-and-retry, but the sandbox MCP Airtable account can't see this base, so the live column couldn't be verified. Plan says it was added by hand 2026-06-19; the dev machine should confirm `CrusadeUnits.Died` exists (checkbox) so `✝ mark died` persists rather than silently dropping. | `render/web/crusade_store.py` (live Airtable base) | — |
| 🔵 OPEN | LOW | **Manual leader override not persistable in roster upload review** — the Phase-5 upload unit-review editor and the dashboard show leader status as parser-derived (`CharN:` prefix). Nicknames + attachments persist via roster metadata, but there's no metadata key for a manual leader flag, so toggling leader can't be saved without a data-layer addition (deliberately out of scope per "don't rebuild the data layer"). | `render/web/src/lib/rosterParse.js`, `render/web/server.py` save-metadata | — |
| ✅ DONE | FEAT | **Units Database interactive filter rail (UI redesign Phase 4)** — the units screen shifted from command-only filtering to interactive controls. `UnitListRich` lifted out of `TerminalBlock.jsx` → new `components/UnitListRich.jsx` + `components/UnitListFilters.jsx`, rebuilt to the `List Units` mockup (faction select · roster filter · points sliders · keyword chips [AND] · role chips [OR] · search · sort · ★ shortlist · M/T/Sv/W/OC rows · faction-tinted tags). All filtering client-side over the existing `list units` payload; row click → `onInject('spec X')` (no router). Star binds to the real `ct_starred_units` (string[]); roster filter uses real `fetchRosters` + `parseRosterUnits`, defensively wrapped → no-ops to "All units" on store outage. **Additive data line:** `loader.get_list` units branch now also returns per-unit `M`, `OC`, `role`, and aggregated/de-noised weapon-rule `keywords` (67 raw → 34 clean). Additive keys only — CLI `unit_list_rich` renderer reads name/T/W/legends/forgeworld, unaffected. esbuild graph clean, 159 py tests green. | `data/combat_terminal/loader.py`; `render/web/src/components/UnitListRich.jsx`, `UnitListFilters.jsx`, `TerminalBlock.jsx` | 2026-06-19 |
| ✅ FIXED | **HIGH** | **DATASHEET tab missing + UNITS tab was static** (fix-pass round 2, 2026-06-19) — `spec [unit]` was merged into the units command-stream and clicking UNITS was pure-nav (showed boot text, not the DB). Split to the mockup's two tabs: `UnitsContext` gained a `variant` ("database"\|"datasheet") prop, mounted twice; UNITS tab now runs `list units` (loads `UnitListRich`+filters), DATASHEET hosts spec sheets + the ★ MY UNITS rail; `spec`/`unit`/`datasheet <unit>` route to datasheet (App early-routing + Terminal fallback both updated); stars sync across both faces via `ct-state-changed`. | `render/web/src/App.jsx`, `components/UnitsContext.jsx`, `components/Terminal.jsx` | 2026-06-19 |
| ✅ FIXED | MED | **Rules browser: "Command Points" duplicated across every category filter** (round 2) — the keyword↔rule merge in `buildEntries` adopted the *referenced* rule's name and consumed it on every `defined_by_rule` cross-reference, so each CP/stratagem keyword became a "Command Points" entry scattered across categories. Now only an **identity** match (keyword key == rule name) adopts the rule's name/effect/cp and consumes it; cross-refs keep their own name and only borrow effect text. CP badge gated to `cp_cost > 0`. | `render/web/src/components/RulesContext.jsx` | 2026-06-19 |
| ✅ FIXED | MED | **Command List omitted modifiers; Aliases didn't show existing ones; 11th-ED buried in "Math Mode"** (round 2) — (a) modifiers live in `flags.FLAG_SPECS` not the query schema, so the Command List now also fetches the engine `legend` and renders a **Combat Modifiers** section (44 flags, verified live). (b) The Aliases tab read only client `ct_aliases`; it now imports the engine's `term_aliases` on load (the user's `deepstrike`/`fireknife` reappear) and syncs add/remove via `learn`/`unlearn`. (c) Settings sub-tab "Math Mode" → **General** (first), now holding Rules Edition + a new Theme picker + Monte Carlo/iterations/confidence/toggles; the `math`/`mathmode` ledger-replay command is untouched. | `render/web/src/components/SettingsPage.jsx`, `App.jsx` | 2026-06-19 |
| ✅ FIXED | LOW | **Diagnostics page wouldn't scroll** (round 2) — inner container used `minHeight:100%` (grew past the viewport; parent `overflow:hidden` clipped it). Bounded to `height:100% + minHeight:0` so its own `overflowY:auto` scrolls; SettingsPage wrapper hardened with `minHeight:0`. | `render/web/src/components/DiagnosticsPage.jsx`, `SettingsPage.jsx` | 2026-06-19 |
| ✅ FIXED | **HIGH** (was LOW) | **Theme-token glows/tints were a silent no-op APP-WIDE** (holistic fix pass, 2026-06-19) — what was logged as a one-off `SpecBannerCard` glow bug (`textShadow: 0 0 12px ${nameColor}55` → `var(--ct-faction-*)55`, invalid CSS, whole declaration dropped) turned out to be **systemic: 120 sites across 33 files** used the same `\`${token}${alphaHex}\`` / `token + "alphaHex"` pattern for glows, tinted borders, and tinted panel backgrounds. Because the bases are all CSS vars (`C.*`, `factionColor()`, `rankColor()`, `THREAT_COLORS`, …), every one produced invalid CSS and was dropped — so the redesign's signature glow/tint aesthetic never rendered (the app read flatter than the mockups). **Fix:** wrapped all 120 in `color-mix(in srgb, <token> N%, transparent)` (N = round(hex/255·100)) — the valid pattern Phase 4's `UnitListRich` already used; themeable, degrades gracefully. Applied via a reviewed regex pass, then verified: all 36 changed files esbuild-parse clean + full `App.jsx` import-graph bundles clean. (color-mix already a project dependency.) Raw-hex-based sites are visually unchanged; var()-based sites now render for the first time. | `render/web/src/components/**/*.jsx` (33 files) | 2026-06-19 |
| ✅ FIXED | MED | **Roster upload rejected real `.ros`/`.rosz` files** (holistic fix pass, 2026-06-19) — the file-drop `accept` filter AND the actual client-side validator (`Terminal.jsx` `["roz","rozs","json"].includes(ext)`) used **typo'd extensions** (`.roz`/`.rozs` — transposed). A genuine BattleScribe/New Recruit export (`.ros` XML / `.rosz` zip) was rejected with "Unsupported file type." Backend parses by content (no extension check), so this was a pure client gate. Corrected to `.ros`/`.rosz` across the validator, both `accept` attrs, and all display copy (mockup uses `.rosz,.ros`). | `render/web/src/components/{Terminal,TerminalBlock,RostersContext}.jsx`, `roster/UploadBlock.jsx`, `ui/file-upload.jsx` | 2026-06-19 |
| ✅ FIXED | LOW | **Threat distribution value column clipped** (holistic fix pass, 2026-06-19) — `ThreatSummary` BY-UNIT-COUNT rows used `60px 1fr 48px`; the 48px value cell could clip "6 (43%)". Widened to `64px 1fr 70px` (mockup parity). | `render/web/src/components/threat/ThreatSummary.jsx` | 2026-06-19 |
| ✅ FIXED | **HIGH** | **Loaded roster shows "No roster loaded" / no units in ACTIVE ARMIES** — reported 2026-06-11, after the Airtable connection was restored. Root cause: session-token mismatch. The engine keys all roster/faction state per `session_id` (the P0-2 per-tab isolation token). The Terminal loads a roster via `App.handleExec`, which sends `session_id: getSessionId()` → state stored under tab session S. But the Rosters page polled the engine with `{input:"session"}` and **no `session_id`** (`RostersContext.fetchSession`), so it read a *different* (empty) session and always rendered "No roster loaded" with zero units — for every roster, including fresh uploads. This was a latent regression: it broke the moment per-tab session isolation landed, since the poll was never updated to pass the token. Fix: `RostersContext.fetchSession` now sends `session_id: getSessionId()`, matching `handleExec`/`crusade.js`. Verified the other two engine `/exec` callers already pass the token; this was the only one missing it. | `render/web/src/components/RostersContext.jsx` | 2026-06-11 |
| 🟠 PARTIAL | **HIGH** | **"Load player" roster fails with no real reason** — reported 2026-06-11. Roster storage is the Airtable `SharedRosters` table in the *same base as profiles* (which is broken prod-side, see the profiles row). The roster API endpoints had **zero error handling**, so any Airtable failure (bad/expired `AIRTABLE_TOKEN`, wrong base, wiped rows, unreachable) fell through as a generic 500, and the client's `findRosterByName` treated *every* non-OK response as `null` → surfaced the misleading **"Roster '<name>' not found"**, hiding a connection/config problem behind a "doesn't exist" message. **Code fix (shipped):** roster endpoints now distinguish 404 (genuinely absent) from **502 with the real Airtable cause** (`_airtable_message`); new `RosterStoreError`; new **`GET /api/rosters/health`** + in-app **`roster health`** command report `{ok, base_id, table, count, error}` for one-shot diagnosis; client `findRosterByName` throws the real detail on store errors (only returns null on 404) and `fetchRosters` echoes the server detail. So the failure is now honest instead of a lie. **Still needs (almost certainly env, not code):** run `roster health` (or hit `/api/rosters/health`) to read the cause; it's the same base as profiles, so verify `AIRTABLE_TOKEN` / `AIRTABLE_BASE_ID` (appfOqXB5mLMgXFKp) on Phil-2 and that the `SharedRosters` table exists. If `count:0`, rows were wiped (restore/re-upload). | `render/web/shared_rosters.py`, `render/web/server.py`, `render/web/src/lib/shared-rosters.js`, `render/web/src/components/Terminal.jsx` | 2026-06-11 (code) |
| ✅ DONE | FEAT | **Conditional weapon keywords are now click-to-apply chips** — Heavy / Rapid Fire / Melta / Lance are parsed off the weapon but only apply when the player declares the situation (stationary / half range / on the charge). Previously the only way to apply them was to know the flag and type it. Now the engine seeds those flags into the attacker flag *universe* (inactive) whenever a weapon carries the keyword — `_query_combat` after `final_all_att`, gated so always-on/additive keywords (esp. Sustained) are never seeded (would stack). The weapon-spec table renders each keyword badge via a new `KeywordBadge`: always-on keywords show as static lit badges (not clickable); the four conditional ones render crossed-out when inactive and lit when active, and clicking fires `rerun --<flag>` / `--<flag> null` (a real engine re-run, since applying them changes hit/wound/damage math). Verified: bare `--ea`-style no-op avoided; Rothail volley gun (RAPID FIRE 3) dmg 0.83→1.67 on apply, back to 0.83 on remove; flag universe never duplicates across reruns; Sustained/Lethal/Blast not seeded. **Stacking note:** confirmed `_merge_mods` makes booleans safe (Heavy/Lethal OR, applied once) but `sustained_hits` is additive — that's exactly why Sustained stays a static badge. New test `test_conditional_weapon_keywords_seeded_inactive_and_toggleable`. Per-weapon `shots` column still shows base attacks when RF is applied (dmg is correct) — minor display gap, logged for later. | `data/combat_terminal/engine.py` `_query_combat`; `render/web/src/components/combat/WeaponStatsTable.jsx`; `render/web/src/components/CombatBlock.jsx`; `tests/test_loader_smoke.py` | 2026-06-11 |
| ✅ FIXED | MED | **Overkill % (and avg dmg/attack) frozen when weapons toggled off** — reported 2026-06-11 (Plagueburst Crawler vs Boyz: 44% overkill unchanged after disabling weapons). Toggling a weapon row doesn't re-run the engine; `CombatBlock.adjustedData` recomputes totals client-side but only adjusted `expected_dmg`/`expected_kills` — `overkill_waste_pct` was passed through unchanged via `...serverData`, so it stayed at the all-weapons value. Fix: `adjustedData` now recomputes `overkill_waste_pct` as the dmg-weighted average of the *enabled* weapons' per-weapon `overkill_pct` (exactly the server's `_summarize_ev` formula), and recomputes `avg_dmg_per_attack` over the enabled set. Verified the client weighted-average reproduces the server (40.4 vs 40.8 MC) and drops to 29.1 with the mortar toggled off. `kill_chance_pct`/`squad_wipe_pct` still pass through — they need the joint MC kill distribution and can't be faithfully recombined client-side (documented in-code; possible future fix using per-weapon `w.mc` kill buckets). | `render/web/src/components/CombatBlock.jsx` `adjustedData` | 2026-06-11 |
| ✅ FIXED | **HIGH** | **Bare `--ea` added 0 extra attacks (no-op)** — reported 2026-06-11. `_apply_flags` parsed the value as `float(re.sub(r'^ea','',key) or 0)`, so the no-number token `"ea"` resolved to `0` and `extra_attacks += 0` — the flag rendered its callout note ("+ extra attack(s) per model") but changed nothing in the math. (Hidden because the existing "bare" test used `ea1`, not a true bare `ea`; the parametric `--ea2`/`--ea:3` forms worked.) Fix: default the no-value form to `+1` (`... or 1`), matching `--sus` and the flag legend; `flags._n_ea` note default `''`→`'1'` so the callout now reads "+1 extra attack(s)". Verified end-to-end: Predator Annihilator `--rrhit1 --lethal --ea` ranged EV 6.12→12.25 (doubles 2× A1 weapons); `--ea2`→18.38. New test `test_ea_flag_no_value_defaults_to_one`; 142/142 math green. | `math_adapter.py` `_apply_flags`, `flags.py` `_n_ea`, `tests/test_combat_math.py` | 2026-06-11 |
| 🟠 PARTIAL | **HIGH** | **Profiles "deleted" on login + "Failed to create profile" (prod)** — reported 2026-06-11. Not a code regression (no profiles commit since the Airtable migration; the round-3/4 math deploys don't touch this path). Root cause is Airtable-side (token/base-id/table/column-type or wiped rows) and was *invisible* because the code hid it: `create_profile` always sent `PinHash`/`PinSalt` as `""` (the exact INVALID_VALUE_FOR_COLUMN trap the Reports table hit if a column's type drifts), the create route caught only `ValueError` so any store error fell through as an opaque 500 → generic UI "Failed to create profile", and the list route returned `[]` on failure → looked like data loss. **Code fix (shipped):** `create_profile` omits empty optional fields; new `ProfileStoreError` + `_airtable_message()` surface the real Airtable cause; create/list/login/state/delete routes now return a clean **502 with the actual message** (never a silent empty list or 500); new **`GET /api/profiles/health`** reports `{ok, base_id, table, count, error}` for one-request diagnosis; frontend surfaces read errors instead of showing an empty list. 2 new API tests. **Still needs (prod, not code):** hit `/api/profiles/health` to read the cause, then verify `AIRTABLE_TOKEN`/`AIRTABLE_BASE_ID` (appfOqXB5mLMgXFKp) on Phil-2 and that the `Profiles` table exists with `CreatedAt`/`UpdatedAt`/`PinHash`/`PinSalt` as Single line text. If `count:0`, the rows were wiped in Airtable (restore from backup or recreate). | `render/web/profiles.py`, `render/web/server.py`, `src/lib/profile.js`, `tests/test_api_integration.py` | 2026-06-11 (code) |
| ✅ FIXED | **CRITICAL** | **Underscore keyword variants silently ignored (559 real weapon instances)** — `_normalise_keywords` only uppercased; the ladder matched space/hyphen forms only, but dossiers contain `DEVASTATING_WOUNDS` ×145, `RAPID_FIRE` ×149, `TWIN_LINKED` ×68, `IGNORES_COVER` ×58, `SUSTAINED_HITS` ×55, `LETHAL_HITS` ×12, plus dice-valued forms (`SUSTAINED HITS D3` ×19, `RAPID FIRE D6` ×7). These weapons computed as plain guns — no error, no degraded flag. Fix: `_normalise_keywords` now canonicalises (`_`→space, tight hyphens, collapsed whitespace); SUS/RF/MELTA regexes accept dice values via `_expected_dice` (D3→2.0 EV; MC rounds per crit — int-guard added at `combat_math_engine.py` `pending_hits`); bare `SUSTAINED HITS`→1; ANTI regex accepts space or hyphen; **bonus: `IGNORES COVER` weapon keyword (299×) wired to `mods.ignore_cover`** (was never connected in any form). Verified against live dossiers: 559 underscore-keyword weapons now produce active modifiers. 14 new tests (`TestKeywordVariantNormalisation`). | `math_adapter.py` `_normalise_keywords`, `_weapon_to_profile`; `combat_math_engine.py` MC loop; `tests/test_combat_math.py` | 2026-06-10 |
| ✅ FIXED | **CRITICAL** | **Rapid Fire N (and `--ea`) applied once per squad, not per model** — `extra_attacks += rf_value` happened once; squad multiplication was separate, so a 10-model A1 RF1 squad with `--rf` fired 11 attacks instead of 20 (45% damage missing); `--ea` was per-model in the display but once-per-squad in the math. Fix: all three math passes (`_run_ev`/`_run_mc`/sensitivity `_run_total`) now scale `merged.extra_attacks` by `att_models` inside the existing squad-multiplication branch (so `_no_multiply` unit-level weapons are correctly exempt; MC volleys path adds extra_attacks once after all volleys, so the same scaling is correct there). Verified: 10×RF1 = exactly 2× no-rf and 10× a single model; MC mean tracks EV. 5 new tests (`TestPerModelAttackBonusScaling`). 108/108 math + 15/15 loader + 19/19 crusade green. | `math_adapter.py` `_run_ev`/`_run_mc`/`_run_total`; `tests/test_combat_math.py` | 2026-06-10 |
| ✅ FIXED | **HIGH** | **`set_mc_trials(0)` (documented MC-disable) crashed the whole combat** — div-by-zero in `monte_carlo_attack` kill_probs + `round(None)` in the summary → "combat math failed outright", discarding valid EV numbers. Fix: `_run_mc` short-circuits when `_MC_TRIALS <= 0` (simulation reports OFFLINE with a "disabled" message, EV numbers come through, `kill_chance_pct: None`); `monte_carlo_attack` raises a clear ValueError for `trials<=0` (API misuse guard); summary round is None-safe. Test: `test_mc_disabled_clean_fallback`. | `math_adapter.py` `_run_mc`/summary, `combat_math_engine.py` `monte_carlo_attack` | 2026-06-10 |
| ✅ FIXED | **HIGH** | **Anti-X wrong twice**: (1) applied vs every target — no defender-keyword gate (`ANTI-FLY 2+` wounded a ground tank on 2+; ~200 weapons overstated); (2) didn't lower the crit-wound threshold, so Anti+Devastating Wounds only bypassed saves on 6s instead of every N+ (big understatement). Fix: keyword Anti is now stored as gated `anti_entries: [(keyword, N)]` on `AttackModifiers`; `TargetProfile` gained `keywords` (populated by `_unit_to_target`, tokenised to survive the `KEYWORDS:`/merged-tail parse artifacts); new `resolve_anti_threshold(mods, target)` applies the best matching threshold in BOTH EV and MC, and lowers the effective crit-wound threshold to N (10e: an unmodified N+ wound roll is a Critical Wound). A directly-set `anti_wound_target` stays unconditional (manual path). Verified on real data: Chainfist `ANTI_VEHICLE_3+` → 1.33 dmg vs VEHICLE, 0.67 vs non-vehicle. 5 new tests incl. EV↔MC agreement. | `combat_math_engine.py` (TargetProfile, AttackModifiers, `resolve_anti_threshold`, EV+MC paths), `math_adapter.py` (`_weapon_to_profile`, `_unit_to_target`) | 2026-06-10 |
| ✅ FIXED | **HIGH** | **Blast implemented the 9th-ed rule** — min-3-attacks-vs-6+-models; 10e is +1 Attack per 5 models in the target unit, no minimum. Fix: all three math passes now add `def_models // 5` to `extra_attacks` (a per-model bonus, so it scales with the firing squad and works in the MC sampled-attacks path — the old `sample_attacks=False` blast override is gone, fixing that EV/MC drift too). Flag note/legend, engine BLAST warning (now 5+ models), and stale min-3 docs all updated. 3 new tests (per-5 scaling, no 9e floor, per-firing-model). | `math_adapter.py` ×3 sites, `flags.py`, `engine.py` | 2026-06-10 |
| ✅ FIXED | **HIGH** | **`criton:N` below the hit target: EV and MC diverged 100%** — crits below the modified target didn't auto-succeed. Fix: `success_probability` now treats the effective target as `min(target, crit_on)` (10e: critical rolls always succeed; save rolls unaffected since targets are clamped ≤6 and crit_on defaults to 6); `crit_probability`'s reroll-failed branch passes `crit_on` through so crits are never counted as rerollable fails; MC rolls against `min(target, crit threshold)` for both hit and wound (wound side uses the anti-aware effective crit threshold). Test: BS6+ criton5 → p_hit exactly 2/6; criton+lethal EV↔MC within 10%. | `combat_math_engine.py` `success_probability`, `crit_probability`, MC loop | 2026-06-10 |
| ✅ FIXED | MED | **MM1 — ±1 net modifier cap on hit & wound rolls** — cumulative hit/wound modifiers now clamp to net +1/−1 (`net = clamp(bonus − penalty, −1, 1)`) in BOTH EV (`compute_attack_result`) and MC (`monte_carlo_attack`). BS5+ with +3 → 4+ (was 2+). Tests: `TestRound3MedFixes.test_hit_net_modifier_cap`, `test_wound_net_modifier_cap`, `test_hit_net_cap_mc_matches_ev`, `test_flag_level_net_cap_equals_single_plus_one`. | `combat_math_engine.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM2 — Torrent weapons score no Critical Hits** — 10e: no hit roll → no crits → Sustained/Lethal never trigger. EV sets `p_hit=1.0, p_crit_hit=0.0` under torrent; MC sets `crit_hit=False` (no crit die rolled). Tests: `test_torrent_no_crit_hits_ev` (6 attacks + sus1 + lethal → exactly 6 hits), `test_torrent_no_crit_ev_mc_agree`. | `combat_math_engine.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM3 — Cover gives no bonus vs AP0 on a 3+ save** — `compute_save_target` applies the cover bonus only when NOT (`effective_ap==0 and save<=3`) (10e Benefit of Cover). Tests: `test_cover_no_bonus_vs_ap0_on_three_plus`, `test_cover_applies_vs_ap1_on_three_plus`, `test_cover_applies_vs_ap0_on_four_plus`; existing `test_cover_increases_save` updated to AP-1 (old behaviour was wrong). | `combat_math_engine.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM5 — `target.models` is the real defender size** — `compute_combat`/`compute_sensitivity` now set `target.models=max(1,def_models)`; EV summary caps `expected_kills` at `def_models`; MC kill buckets extend past 1. `_kills` test helper updated to `def_models=10` so the cap doesn't flatten its differential. Tests: `test_expected_kills_capped_at_def_models`, `test_kill_buckets_extend_past_one`. | `math_adapter.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM7 — `--melta:N` overrides, does not stack** — added `melta_override: Optional[int]` to `AttackModifiers` (None-merge-safe via `_merge_mods`); `_apply_flags` sets the override (+`use_melta`) instead of `melta_value`; the three application sites use `mv = override if not None else melta_value`. Weapon Melta 2 + `--melta2` = Melta 2 (no doubling). Tests: `test_melta_flag_overrides_keyword`, `test_bare_melta_applies_keyword_value`. | `combat_math_engine.py`, `math_adapter.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM6 — `--lance` gated on the LANCE keyword** — added `is_lance` weapon bit (set by the LANCE keyword) + `use_lance` flag bit (set by `--lance`), gated `if use_lance and is_lance: wound_bonus += 1` at the three math sites (mirrors heavy). Flag no longer buffs every weapon. `flags.py` note → "+1 to Wound for LANCE weapons (on the charge)". Tests: `test_lance_flag_only_buffs_lance_weapons`; `test_lance_flag`/`test_lance_keyword` updated to the gated behaviour. | `combat_math_engine.py`, `math_adapter.py`, `flags.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM10 — math ledger reads the engine's computed values** — `math_adapter` now stamps `save_target` (from `AttackResult.save_target`) into `per_weapon_dmg`; `build_combat_ledger` reads the stamped target (invuln-, AP-, save-flag- and cover-aware) instead of re-deriving it, and drops the cover-bonus line when `ml`/`igncover`/`nocover` is active. Test: `test_ledger_save_event_matches_engine_for_invuln_unit`. | `math_adapter.py`, `math_ledger.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM4 — EV kill math applies Feel No Pain per damage point** — the old two-branch formula rolled one FNP per whole wound and ignored the discrete wounds-per-model threshold + overkill, diverging −27%…+39% from Monte Carlo on D3/D6 damage vs FNP. Replaced with a per-wound surviving-damage distribution + a small Markov chain over the defender's remaining HP (`_expected_kills`/`_survivor_distribution`/`_damage_outcomes`); EV↔MC now agree within ~7% across regimes, and the no-FNP one-shot case stays exact. Tests: `TestRound4MathFixes.test_fnp_*`, `test_no_fnp_oneshot_kills_preserved`. | `combat_math_engine.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM9 — multi-profile weapons no longer double-summed** — strike/sweep and standard/supercharge profiles (and any `Name – profile` pair) are grouped by base name and collapsed to the single best profile (highest expected kills at the real squad size, damage tiebreak) vs the current target, before the EV and MC passes. Singletons pass through unchanged. Verified on the real Emperor's Champion Black Sword. Tests: `test_multiprofile_not_double_summed`, `test_multiprofile_picks_best_for_target`, `test_single_profile_weapon_unaffected`. **Still open (separate rule):** a model firing Pistols *instead of* its other ranged weapons (model-level either/or in engagement range) is not modelled. | `math_adapter.py` | 2026-06-11 |
| ✅ FIXED | MED | **MM11 — defender attached leader folded into target unit size** — a resolved defender leader was used only for the display name; it now adds its model(s) to `def_models`, so the target unit's size feeds Blast scaling + the kill cap, and a transparency note states the leader's own defensive profile (W/Sv/invuln, granted abilities) isn't separately modelled. Test: `test_loader_smoke.test_defender_attached_leader_counts_in_unit_size`. **Still open (separate, design-heavy):** modelling the leader's distinct W/Sv/invuln and unit-wide granted abilities (mixed-profile defence + wound allocation). | `engine.py` | 2026-06-11 |
| ✅ FIXED | **HIGH** | **`list units` showed collapsed/duplicate names; Tau Legends/Forge World untagged; current Kroot missing.** Three root problems, all fixed at the data layer (the prior "fix" only added an inert `legends:false` field and de-truncated abilities — it never deduped names, never set any legend true, and never added Kroot). (1) **Dedup across all 11 affected factions** (`/tmp/dedup.py` migration): the parser had collapsed variant datasheets to a base name (Tau `COMMANDER` ×3, Orks `BIG MEK` ×3 / `WARBOSS` ×3, SM `CAPTAIN` ×6, etc.). Precise names recovered from the `■` token in `unit_composition`, with curated overrides for squad/truncation edge cases (Tyranid Warriors melee/ranged via keyword, Wolf Guard variants, Assault/Vanguard/Death Company "with Jump Packs", Logan Grimnar on Stormrider, Ahriman/Exalted Sorcerer "on Disc of Tzeentch" verified vs Wahapedia). 4 genuine duplicate parses dropped keeping the richer copy (3 Orks: Big Mek KFF / Nobz on Warbikes / Warboss on Warbike; 1 SM: Sanguinary Priest). Result: 0 duplicate names in any faction. (2) **Tau tags**: `legends:true` on the 10 current-Legends units (Aun'shi, Aun'va, Commander in Crisis Battlesuit, Longstrike, Barracuda, Remora Stealth Drones, R'varna, Tetras, Y'vahra, Tactical Drones) + retired generic Kroot Shaper; new `forgeworld:true` on the 4 FW units (AX-1-0 Tiger Shark, Manta, Tiger Shark, Ta'unar). (3) **Added 5 current Kroot** from Wahapedia (Lone-Spear, Flesh Shaper, Trail Shaper, War Shaper, Krootox Rampagers). Flags now flow through `loader.get_list` projection; web `UnitListRich` + CLI `unit_list_rich` render `[LEGENDS]`/`[FORGE WORLD]` badges (CLI previously fell through to a raw JSON dump — also fixed). 104 loader/math tests pass; frontend builds clean. | `data/*/...parsed_dossier.json` (all factions), `loader.py`, `render/web/src/components/TerminalBlock.jsx`, `render/cli/terminal.py` | 2026-06-05 |
| 🔶 OPEN | MED | **Parser-doubling / partial datasheets in the kitchen-sink `space marines` file (217→216 units) and elsewhere.** The dedup pass exposed that the underlying parser sometimes emits a datasheet twice (once cleanly, once with a `KEYWORDS:`-prefixed keyword artifact and divergent partial weapon/ability lists). Handled the 4 cases that produced literal duplicate *names*, but other same-name-collision-free doubles may persist with merged-but-incomplete loadouts. Also: the `space marines` megafile aggregates chapter units (Space Wolves/Blood Angels) that have their own faction dirs — possible cross-file duplication worth a dedicated audit. Wolf Guard variant names ("Pack Leaders in Terminator Armour" etc.) were best-effort from stats/keywords; verify exact wording vs Wahapedia. | parser upstream; `data/factions/space marines/...` | — |
| ✅ FIXED | MED | **`report` command 422'd when the `Reports` Airtable table existed but a column was typed as Date** — `create_report` always sent `ResolvedAt: ""` (plus `Command`/`Faction` as `""`). Airtable rejects an empty string for a Date/number column (`INVALID_VALUE_FOR_COLUMN: Cannot parse date value ""`), so a report failed whenever `ResolvedAt`/`CreatedAt` got auto-typed as Date (e.g. from a CSV import that inferred types from sample timestamps). Fix: optional fields are now only included in the create payload when non-empty (`ResolvedAt` omitted entirely on create; `Command`/`Faction` omitted when blank), so the writer no longer depends on exact column typing — a blank cell is accepted by every field type. `resolve_report` already sent a valid timestamp, unchanged. Verified the payload omits empties and includes populated optionals. **Operational root cause (separate from the code):** the prod Airtable PAT lacks `schema.bases:write`, so `reports._ensure_schema` silently no-ops and never auto-creates the `Reports` table — it had to be created by hand (table `tbl7jvYQJ2ghP0DkW` in base `appfOqXB5mLMgXFKp`), with `CreatedAt`/`ResolvedAt` set to Single line text per `REPORTS_TABLE_SPEC`. | `render/web/reports.py` `create_report` | 2026-06-05 |
| 🔶 OPEN | LOW | **Airtable schema auto-provisioning is inert in prod** — `reports._ensure_schema` (and the equivalent expectation for the Crusade/Profiles/Rosters tables) requires the PAT to carry `schema.bases:write`; the prod token doesn't have it, so a missing table fails silently and surfaces only as a 403/422 at write time. Options: (a) grant the prod PAT `schema.bases:write` so tables self-provision on first use, or (b) keep CSV templates for manual creation. A fresh base would currently need every table built by hand. A `Reports.csv` import template (exact field names/order) was produced 2026-06-05 — remember Airtable Date auto-typing: keep `CreatedAt`/`ResolvedAt` as Single line text. | `render/web/reports.py`, `crusade_store.py`, `profiles.py`, `shared_rosters.py` | — |
| ✅ DONE | **FEATURE** | **Variable squad size — `--n` / `--models` override with legal range.** Many units field a variable model count (Kroot Carnivores 10–20, Intercessors 5–10, Boyz 10–20); previously combat could only use a loaded-roster size or the dossier minimum, with no ad-hoc way to set it. (1) `_parse_size_range(composition) → (min, max)` reads the range the dossier already encodes (`'10-20 Kroot Carnivores'`); `_parse_min_models` delegates to its min half. (2) New override flag `--n N` / `--models N` (forms `--n20`, `--n 20`, `--models:20`), parsed by `_size_flag_value`/`_extract_size_flags` — matched precisely so it never collides with real flags (`nocover` ≠ size `n`). Size tokens are stripped from every flag list before `validate_flags`/the math/the modifier-toggle UI (size is structural, not a modifier). (3) **Precedence: explicit override > loaded roster > dossier minimum** (per the build decision). Attacker `--n` before `vs`, defender after. (4) The override persists across `rerun` (stored explicitly in `last_combat` since it's stripped from the flags). (5) Combat surfaces the active size + range as a callout, warns when an override is outside the legal range (uses it anyway — it's a what-if tool), and the "assumed from dossier minimum" note now shows the range + the `--n` hint. Result data exposes `att_models`/`def_models`/`*_size_range`/`*_models_override` for a future size chip/control. Legend documents it. 4 new tests (range parser ↔ min consistency, flag detection vs real flags, override-beats-roster, rerun-persist + out-of-range warn). | `engine.py` `_parse_size_range`, `_size_flag_value`, `_extract_size_flags`, `_query_combat`, `_query_rerun`, `_query_legend`; `tests/test_loader_smoke.py` | 2026-06-05 |
| ✅ FIXED | **HIGH (P0-2)** | **Single global engine session shared across all requests** — `engine._session` was per-instance state on the one shared engine held by `EngineRegistry`, mutated by every `/exec` and `/query`; two browser tabs or two users collided (one tab's `faction`/`disambiguation`/`last_combat`/rosters clobbered another's; an interleaved request made `rerun` apply to the wrong combat). **Confirmed multi-user/multi-tab is real, so this was built (not skipped).** Fix: session state is now keyed by an opaque client token. `_session` became an `@property` resolving to `self._sessions[sid]` where `sid` lives in a `threading.local` (FastAPI runs sync routes in a worker-thread pool, so a thread-local cleanly separates concurrent requests); `set_session(token)` is called by `server.py` at the top of both `/exec` and `/query` before any mutation; `query()` wraps dispatch in a per-session `RLock` (reentrant for the `exec`→`query` path) so two in-flight requests from the *same* client can't tear `_session`. Callers that set no token (CLI, tests, direct calls) fall back to one shared `_default` session — pre-P0-2 behaviour. Frontend: new `lib/session.js` mints a per-tab token in `sessionStorage`, sent by `App.handleExec` and by `crusade.js startBattle` (same token, so a mustered crusade roster reaches the terminal). New API test asserts two session ids don't see each other's faction/last_combat. Verified: global RNG stream is byte-identical before/after a combat (MC no longer leaks); no `self._session =` rebinds exist that would break the property. | `engine.py`, `server.py`, `src/lib/session.js` (new), `src/App.jsx`, `src/lib/crusade.js`, `tests/test_api_integration.py` | 2026-06-05 |
| ✅ FIXED | **HIGH (P1)** | **`_load_faction` marked a faction loaded even on JSON parse failure** — a parse/read error appended to `_errors` but still `self._loaded_factions.add(faction_key)`, so the faction was permanently empty and never retried (every later lookup silently returned nothing). Fix: a parse failure no longer marks the faction loaded (so a subsequent access retries, e.g. after the file is fixed) and the error is deduped to avoid log spam. New loader smoke test points a faction at a broken JSON, asserts it isn't marked loaded + is logged once, then asserts a fixed file loads on retry. | `loader.py` `_load_faction`, `tests/test_loader_smoke.py` | 2026-06-05 |
| ✅ FIXED | **HIGH (P1)** | **Unguarded `int(models)` crashed combat on junk roster data + brittle squad-size parse silently under-scaled squads.** (1) Roster `models` like `"3+"`, `"10 models"`, `null` or a float crashed the four `int(...)` sites in `_query_combat`; replaced with a never-raising `_coerce_models(value, fallback)` (extracts the first positive int, excludes bool, falls back to the dossier minimum). (2) `_parse_min_models` only counted ■-bulleted lines, so squads whose composition lists models on plain lines (`'1 Intercessor Sergeant'`, `'4-9 Intercessors'`) scaled as **1 model** — a 5× under-count of all their damage. Rewrote it to read counts regardless of bullet while excluding the points table (`'5 models\t80'`) and equipment/rules prose, and requiring an upper-case word after the count so lower-case weapon options (`'1 killkannon'`) aren't miscounted. Verified on real dossiers: Intercessor Squad→5, Blood Claws→10, Flash Gitz→5, Battlewagon→1. (3) When a count is the dossier minimum (a guess) rather than an explicit roster size, combat now surfaces a "model count assumed" callout (and a specific BLAST-won't-fire note when blast targets an assumed <6-model unit) so MC confidence isn't presented as authoritative for a size we assumed. New tests for `_coerce_models` and `_parse_min_models`. | `engine.py` `_coerce_models`, `_parse_min_models`, `_query_combat`, `tests/test_loader_smoke.py` | 2026-06-05 |
| ✅ FIXED | MED (P1) | **Global RNG reseed clobbered dice independence** — `monte_carlo_attack` did `random.seed(42)` per weapon and `_query_dice` drew from the same module-global `random`, so rolling a combat then rolling dice produced correlated results. Fix: `monte_carlo_attack` now uses a local `random.Random(seed)` (preserving the seed=42 reproducibility of the confidence bands while never touching the global stream) threaded through `roll_d6`/`_passes_roll`/`_reroll_mode`/`roll_dice_expression` (each gained an `rng` param defaulting to the global for back-compat); `_query_dice` uses its own `random.Random()`. Verified the global stream is identical before/after a combat; 89/89 math tests still green (MC determinism preserved). | `combat_math_engine.py`, `engine.py` `_query_dice` | 2026-06-05 |
| ✅ FIXED | MED (P1) | **API security hardening (cluster).** (1) **SPA fallback path traversal** — `dist_dir / full_path` → `FileResponse` had no containment check; now `(dist_dir/full_path).resolve()` must be `is_relative_to(dist_root)` before serving (blocks `../../etc/passwd`). (2) **Delete-route ownership** — `delete_shared_roster` ignored its `requester` arg; it now enforces uploader-only deletion when the roster has a known uploader and a requester is supplied (frontend sends the profile name as a non-secret query param). (3) **PIN out of the URL** — `DELETE /api/profiles/{name}` took the PIN as a query param (lands in access logs); moved to the request body (`ProfileDeleteBody`). (4) **Airtable formula injection** — every raw f-string `formula=` site in `crusade_store.py` now routes through `_eq_formula`/`_escape_formula_value` (escapes backslash + quote); `shared_rosters.py` and `profiles.py` formula lookups escaped too (the latter defence-in-depth; already slug-sanitised). | `server.py`, `shared_rosters.py`, `crusade_store.py`, `profiles.py`, `src/lib/shared-rosters.js` | 2026-06-05 |
| ✅ FIXED | LOW (#3) | **Legend hand-maintained a 5th copy of every flag's description/effect** — `_query_legend` carried separate `offensive_modifier_flags`/`defensive_modifier_flags` lists that could drift from the parsing/classification/notes in `flags.FLAG_SPECS`. Added `flags.legend_rows()` which generates both columns from the registry (effect text reuses the same callout-note builders shown in combat), and `_query_legend` now consumes it. New test asserts every `FLAG_SPECS` flag appears in the legend output and that the offensive/defensive split matches the registry. (Cosmetic: the generated legend now also lists alias flags and shows parametric flags by bare stem — display-only, math unaffected.) | `flags.py` `legend_rows`, `engine.py` `_query_legend`, `tests/test_loader_smoke.py` | 2026-06-05 |
| ✅ FIXED | **HIGH (P0-4)** | **Monte Carlo ignored damage-dice (and attacks-dice) variability** — `WeaponProfile` parsed `damage_expression`/`attacks_expression` but `monte_carlo_attack` never read them, so a `D6`-damage weapon and a flat-3.5 weapon produced identical MC distributions and confidence bands (the sim advertised variance it wasn't modelling). Fix: added `roll_dice_expression()` to `combat_math_engine.py` and made `monte_carlo_attack` *sample* the damage expression per unsaved wound and the attacks expression per trial when `damage_is_variable`/`attacks_is_variable` are set; non-variable weapons are byte-for-byte unchanged. Variable attacks respect squad size via a new `attacks_volleys` param (one expression roll per firing model) instead of pre-multiplying the EV; `_run_mc` passes it and falls back to the EV path under the Blast min-3 floor. EV (displayed `expected_dmg`) is unchanged — only the MC spread/margin/swinginess move. New `TestMonteCarloVariance`: D6 damage spreads ≥1.3× wider than equal-EV flat, D6 attacks spread wider than flat, and the mean is preserved. | `combat_math_engine.py` `monte_carlo_attack`, `roll_dice_expression`; `math_adapter.py` `_run_mc` | 2026-06-05 |
| ✅ FIXED | **HIGH (P0-5)** | **Embedded `--dmgplusN` extra-damage flag silently did nothing** — surfaced by the new flag-registry sync test. `_apply_flags`'s ed/dmgplus branch matched `^ed\d+$` but not `^dmgplus\d+$`, and its value extractor only read the `ed` stem, so `--dmgplus2` fell through with zero effect (only bare `--dmgplus` / `--dmgplus:N` worked). Generalised the branch to `^(ed|dmgplus)\d+$` and to read the correct stem length. This is exactly the silent-desync class P0-5 targets. | `math_adapter.py` `_apply_flags` | 2026-06-05 |
| ✅ FIXED | **HIGH (P0-5)** | **De-duplicated the Strength-vs-Toughness wound chart and the modifier-flag logic.** (1) Wound chart: the three divergent copies (`combat_math_engine.wound_target`, `engine._baseline_wr`, `math_ledger._s_vs_t_target`) now all delegate to a single canonical `wound_target(s,t)` + a parsing wrapper `wound_target_from_raw()` in `combat_math_engine.py`. A new `TestWoundTableSync` locks all delegators to agree across a 14×14 S/T grid. (2) Flags: created `data/combat_terminal/flags.py` — one registry (base → parametric? → defensive? → note → legend) that is now the single source for flag integer parsing (replaces the duplicate `engine._flag_int` and `math_adapter._suffix_int`), the callout-note text (replaces `engine.FLAG_NOTE_MAP` + the ~90-line inline elif ladder in `_query_combat`), `KNOWN_FLAG_BASES`, `DEFENSIVE_FLAG_BASES`, and `is_defensive_flag` (all now derived/re-exported). New `TestFlagRegistrySync` asserts: KNOWN/DEFENSIVE sets equal the registry, **every** registry flag produces a real change in `_apply_flags` (no silent flags), every registry flag validates, and parametric note text echoes the same N the math consumes (`sus3`→3, `invuln4`→4+, `eapdef2`→-2). Verified: KNOWN/DEFENSIVE sets and all notes byte-identical to the old behaviour; real combat `intercessors vs intercessors --ml --dev --invuln4` renders the same notes. 85/85 math tests (was 76) + 16/16 crusade green. | `flags.py` (new), `combat_math_engine.py`, `engine.py`, `math_adapter.py`, `math_ledger.py`, `tests/test_combat_math.py` | 2026-06-05 |
| ✅ FIXED | **CRITICAL** | **Failed data load fabricated plausible-but-wrong units into combat math** — when no faction dossier loaded (empty `_units`), `loader.get_unit()` returned `_stub_unit()` — a hardcoded generic bolter Marine — which flowed straight into `compute_combat` and produced confident, fictional damage numbers. `status().ready` still reported `True`. Fix: (1) `_query_combat` now refuses any matchup where attacker or defender resolved to a `_stub` unit, returning a clear "faction data unavailable — combat math disabled" error + an `issue_log` entry; (2) `engine.status()` gates `ready` (and the DIAG pipeline `dossiers`/`engine`/`output` stages) on `total_units > 0`, and appends a "no unit data loaded" error when discovery found nothing; (3) added a not-found guard so a combat where either side is unresolved returns a clean "could not find X" error instead of an empty combat block (no weapons / null math). Verified: real combat unaffected, stub path → error + `ready=False`, bogus name → clean not-found error. 92/92 tests green. | `engine.py` `_query_combat`, `status`; `loader.py` | 2026-06-05 |
| ✅ FIXED | **HIGH** | **Per-weapon math failures silently dropped, surfacing as confident-but-wrong numbers** — `math_adapter._run_ev` / `_run_mc` / `_run_total` and `engine._compute_threat_score` caught bare `Exception` and continued with `None`/`0.0`/skip, so a malformed weapon or a real logic bug was indistinguishable from "weapon did 0 damage" and just vanished from the table. Fix: added a `combat_terminal.math` / `combat_terminal.engine` logger; every dropped weapon is now `logger.warning`-logged. `compute_combat` accumulates a `weapon_errors[]` list (weapon, phase, error) and returns it; `_query_combat` logs each to the session issue log and exposes `weapon_errors` + a `degraded` boolean on the combat result for the UI. One bad weapon still doesn't crash the whole combat (resilience preserved) but is no longer invisible. Verified end-to-end with a forced math failure. 92/92 tests green. | `math_adapter.py`, `engine.py` | 2026-06-05 |
| ✅ FIXED | LOW | **Dead stacked `@staticmethod`** decorating nothing above `_stub_enhancement` (editing accident). Removed. | `loader.py` | 2026-06-05 |
| ✅ FIXED | MED | **Attacker-side colon flags returned empty weapons** — a `--flag:value` token *before* `vs` (e.g. `intercessors --invuln:4 vs broadside`, `--sustained:2`, `--eap:3`) yielded `weapons: []` and the combat block rendered nothing (every parametrised flag affected: ea/ed/fnp/melta/criton/critwound/hitplus/wndplus/invuln/eap). Root cause: `_extract_all_flags`'s pattern `--(\w+)…` — `\w` excludes `:`, so `--invuln:4` captured only `invuln` and left `:4` behind in the text, which then corrupted the attacker's unit/loadout resolution. Fix: pattern now captures an inline colon value `--(\w+(?::-?\w+)?)…` and skips re-appending a space value when the name already carries a colon. Now colon, embedded (`--invuln4`), and space (`--invuln 4`) forms all resolve identically on both sides of `vs`, including negatives (`--eap:-1`) and multi-word unit names. 69/69 math tests green. | `engine.py` `_extract_all_flags` | 2026-06-04 |
| ✅ FIXED | HIGH | Unit stats stored as nested `unit["stats"]` dict but engine reads top-level keys — all spec/threat/math returned `None` for M/T/Sv/W/Ld/OC | `loader.py` `_load_units` | 2026-03-22 |
| ✅ FIXED | HIGH | Weapon profiles in `_query_combat` used wrong key names (`"attacks"`, `"shots"`) — dossiers store attacks as `"a"`, BS as `"bs_ws"`, etc. Result: all weapons showed `? shots` and no profile data in UI | `engine.py` `_query_combat` | 2026-03-22 |
| ✅ FIXED | HIGH | **AP math sign error** — `compute_save_target` used `- effective_ap` in the armor formula, causing more AP to reduce damage instead of increase it (AP-2 vs 3+ showed 17% unsaved instead of 67%). Fixed to `+ effective_ap`: AP now correctly makes saves harder (AP-2 vs 3+ → need 5+ to save = 67% fail). Also affects sensitivity sweep: "AP −1" now shows positive damage gain. | `combat_math_engine.py` `compute_save_target` | 2026-03-24 |
| ✅ FIXED | MED | **Markerlights flag note showed only Ignores Cover** — note text didn't mention the +1 to Hit rolls (which was already correctly wired in the math). Flag note now reads "+1 to Hit rolls, Ignores Cover". | `engine.py` `FLAG_NOTE_MAP` | 2026-03-24 |
| ✅ FIXED | MED | **Drones absent from unit weapon profiles** — spec and combat views for units like Breacher Team, Broadside Battlesuits, Pathfinder Team, etc. showed no drone weapons or stat notes. Added `_DRONE_CATALOG` (gun drone, missile drone, shield drone, guardian drone, marker drone, pulse accelerator, grav-inhibitor, recon, hover drones) and `_UNIT_WEAPON_SUPPLEMENTS` (Breacher Team support turret). `_augment_unit_with_supplements()` runs at spec and combat time, injecting drone weapons (tagged `_drone=True`) and cyan stat notes into the result. Drone weapons display with cyan name tint in both spec WeaponsPanel and combat WeaponStatsTable. | `engine.py` | 2026-03-24 |
| ✅ FIXED | LOW | **Range column missing from combat WeaponStatsTable** — spec view already showed Range; combat view only showed A/BS/S/WR/AP/D. Added "Rng" column and `rangeDisplay` cell to `WeaponStatsTable.jsx`. colSpan for section dividers updated from 7 to 8. | `WeaponStatsTable.jsx` | 2026-03-24 |
| ✅ FIXED | HIGH | **Ability index never built** — `loader.get_ability()` searched `self._abilities` dict that was never populated; all `ability <name>` commands returned stubs. Added `_build_ability_index()` called in `load()`. Indexes by ability name (normalised): CORE/FACTION/CHARACTER entries use `summary` as the key (e.g. "Deadly Demise D3"), regular entries use `name`. Tracks `units[]` and `factions[]` per ability for the result block. | `loader.py` `_build_ability_index` | 2026-03-30 |
| ✅ FIXED | MED | **Commander disambiguation ambiguous** — 3 COMMANDER entries in Tau dossier all showed `COMMANDER  (Tau)` with no way to distinguish. Disambiguation now detects label collisions and appends the first non-CORE/FACTION ability as a hint: `COMMANDER  (Tau)  [Coldstar Commander]`. | `engine.py` `_disambiguate` | 2026-03-30 |
| ✅ FIXED | MED | **CORE/FACTION ability display truncated in spec view** — SpecAbilitiesBlock showed "CORE" as header and "Deadly Demise D3" as collapsed body text, instead of "CORE Deadly Demise D3" as a single readable line. Added `isCategoryTag` detection; category abilities now format as `{tag} {summary}` in the header with no separate body. | `SpecAbilitiesBlock.jsx` | 2026-03-30 |
| ✅ FIXED | MED | **Support turret attacks multiplied by model count** — Breacher Team support turret (1 per unit) was being multiplied by `att_models` (10), inflating attack count to 30. Added `_no_multiply: True` flag to supplement weapon entries; `_query_combat` now skips multiplication for unit-level weapons. | `engine.py` `_UNIT_WEAPON_SUPPLEMENTS` + `_query_combat` | 2026-03-30 |
| ✅ FIXED | LOW | **FLAG_NOTE_MAP incomplete** — only `ml`, `cover`, `dev` had callout notes. Added `lethal`, `twin`, `sustained`, `blast`, `rf`. Invuln note improved to include the save value. Added `zap`/`lightning` to WarningBlock icon map. | `engine.py` `_query_combat`, `WarningBlock.jsx` | 2026-03-30 |
| ✅ FIXED | MED | **No weapon toggle in unit vs unit** — clicking a weapon had no effect; all weapons always contributed to totals. Added toggle-on-click to WeaponStatsTable rows: disabled weapons show strikethrough + "OFF" badge + 35% opacity. CombatBlock manages `disabledWeapons` Set state; filters out disabled weapons from TargetingOutcome and CalcList; recomputes adjusted expected_dmg by summing only enabled weapon per-weapon dmg values. | `WeaponStatsTable.jsx`, `CombatBlock.jsx` | 2026-03-30 |
| ✅ FIXED | MED | **Load roster flow simplified** — `load roster` now has three paths: (1) plain `load roster` → flat numbered list → role_assign (Player/Enemy); (2) `load roster my` → faction list → faction rosters → auto-assign player; (3) `load roster enemy` → same but auto-assign enemy. New flow types `load_roster_pick` and `load_roster_faction` replace the old action-menu path. `load roster <name>` now skips the action menu and goes directly to role_assign. | `Terminal.jsx`, `vfs.js` | 2026-03-30 |
| ✅ FIXED | MED | **Roster → engine faction sync** — `set roster player/enemy <name>` now fires `faction <slug>` / `enemy <slug>` to the engine after assigning VFS roster, so `_session["faction"]` / `_session["enemy_faction"]` stay current. Unit lookups auto-prefer the session faction without requiring `--faction` flags. | `Terminal.jsx`, `engine.py` | 2026-03-30 |
| ✅ FIXED | MED | **Faction-aware unit disambiguation** — `_query_spec` and `_query_combat` now check `_session["faction"]` / `_session["enemy_faction"]` before doing cross-faction search. If the unit is unambiguous within the session faction, it resolves immediately. Only falls back to global multi-faction disambiguation when the unit is absent from or ambiguous within the session faction. | `engine.py` `_query_spec`, `_query_combat` | 2026-03-30 |
| ✅ FIXED | HIGH | **Rosters page "WAIT" hang** — clicking Load Player, Load Enemy, or any action chip on the Rosters dashboard injected a command into the global CommandBar, which routed it to `pendingCommands.rosters`. But `RostersContext` is a dashboard panel (not a Terminal) and never accepts `pendingCommand` / `onPendingCommandConsumed` props, so the command was never consumed, `cmdBarLoading` stayed true, and the CommandBar was permanently stuck showing "WAIT" with input disabled. Fixed: `handleGlobalCommand` in `App.jsx` now detects non-terminal contexts (`rosters`, `diag`) and routes commands to the `main` terminal. One-shot commands (`set roster`, `clear roster`) stay on the current page; all others switch to main so the user can interact with multi-step flows. | `App.jsx` | 2026-06-03 |
| ✅ FIXED | MED | **`session` command rendered as raw JSON** — `result_type: "session_summary"` had no matching case in `TerminalBlock.jsx`. Added `TerminalSessionBlock`: structured layout showing turn badge, state grid (roster mode, player/enemy faction, mods, campaign), two roster panels (player/enemy with unit lists and points), and quick-action chips. Faction names are clickable → injects `list units <faction>`. Units within rosters are clickable → injects `spec <unit>`. | `TerminalBlock.jsx` | 2026-03-31 |
| ✅ FIXED | MED | **CLI focus lost on page click** — clicking terminal output, roster panels, or anywhere on the page blurred the command input, breaking continuous typing. Added a global `mousedown` handler in `App.jsx` that calls `cmdBarRef.current?.focus()` after any click on non-interactive content (skips INPUT, TEXTAREA, SELECT, BUTTON, A, LABEL, contenteditable). Exposed `focus()` via `useImperativeHandle` on `CommandBar`. | `App.jsx` | 2026-03-31 |
| ✅ FIXED | MED | **Diagnostics impossible unit counts (e.g. 221/72 for Space Marines)** — `KNOWN_FACTION_COUNTS` in `DiagnosticsPage.jsx` held stale expected counts from an old data snapshot. `DataIntegrityGrid` also failed to look up factions whose folder names use underscores (`adepta_sororitas`) while the reference table used spaces. Fixed: (1) normalise `perFaction` keys to lowercase-spaces before lookup; (2) updated all 29 reference counts to match actual loaded dossier unit counts as of 2026-03-31. | `DiagnosticsPage.jsx` | 2026-03-31 |
| ✅ FIXED | LOW | **`faction` command added** — new `faction <name>` command sets `_session["faction"]` (player side). Removed erroneous alias mapping of `faction` → `enemy` that caused `faction tau` to set the enemy faction instead. `faction` is now a first-class session command. | `engine.py`, `commands.py` | 2026-03-30 |
| ✅ FIXED | MED | Faction folder names use spaces (`"adeptus custodes"`) but dossier filenames use underscores (`adeptus_custodes_parsed_dossier.json`) — fuzzy match works but normalisation is inconsistent | `loader.py` `_load_units` | — |
| ✅ FIXED | HIGH | **`spec beast of nurgle` (and plural/singular variants) returned no unit** — exact and substring match both fail when query is singular (`"beast of nurgle"`) but stored name is plural (`"BEASTS OF NURGLE"`). Added a third matching pass (word-token) to both `get_unit()` and `get_units_matching()`: strips stop-words (of/the/a/an) and requires all remaining query tokens to appear anywhere in the normalised unit name. | `loader.py` `get_unit`, `get_units_matching` | 2026-04-02 |
| ✅ FIXED | HIGH | **`list chaos daemons` returned "Unknown list type 'chaos'"** — `parse_command` took the first word after `list` as the list type, so multi-word faction names (e.g. "chaos daemons") were split incorrectly. Added a valid-type check: if the first word is not in `{units, weapons, factions, stratagems}`, treat the entire remainder as the faction name and default type to `"units"`. | `engine.py` `parse_command` | 2026-04-02 |
| ✅ FIXED | MED | **`threat beast of nurgle` showed unknownSTUB instead of a helpful error** — `_query_threats` accepted any string as a faction query; when no faction matched it silently returned a stub threat_view with zero units. Added unit-detection fallback: when `get_faction_units` returns nothing, `get_units_matching` is called on the same input; if a unit is found the command returns a clear redirect message — "X is a unit (Faction), not a faction. Try: threat <faction> or: analyze <unit>." | `engine.py` `_query_threats` | 2026-04-02 |
| ✅ FIXED | HIGH | `spec ranger` (and any ambiguous query) returned no result or wrong unit — engine had no disambiguation logic | `loader.py` + `engine.py` | 2026-03-22 |
| ✅ FIXED | **CRITICAL** | **Modifier flags not wired into math engine** — `--lethal`, `--twin`, `--sustained1`, `--dev`, `--blast`, `--rf`, `--torrent`, `--lance`, `--fnp:N`, `--dmgplus:N` flags appeared in FLAG_NOTE_MAP and the UI toggle system but had NO effect on combat math. `compute_combat` and `compute_sensitivity` only handled `ml`, `cover`, `invuln`, `ea`. Refactored into `_apply_flags()` shared helper; all flags now correctly set `lethal_hits`, `reroll_wounds`, `sustained_hits`, `devastating_wounds`, `use_blast`, `use_rapid_fire`, `use_torrent`, `wound_bonus`, `flat_damage_bonus`, `feel_no_pain` etc. on the live `AttackModifiers` / `TargetProfile`. 69-test suite verifies every flag changes damage in the expected direction. | `math_adapter.py` `_apply_flags`, `compute_combat`, `compute_sensitivity` | 2026-04-02 |
| ✅ FIXED | **CRITICAL** | **Multi-model unit math computed per single model** — `compute_combat` never multiplied weapon attacks by model count; a 3-Broadside squad had the same EV as 1 Broadside. `att_models` (already computed in `_query_combat`) now passed to `compute_combat(att_models=N)` and `compute_sensitivity(att_models=N)`. Both `_run_ev` and `_run_mc` inner functions multiply `wp.attacks × att_models` before running probability math, respecting `_no_multiply` for unit-level weapons (support turrets). Verified 3× models → exactly 3× damage in test suite. | `math_adapter.py` `compute_combat`, `compute_sensitivity`, `engine.py` `_query_combat` | 2026-04-02 |
| ✅ FIXED | MED | **`issues` command `entries` always empty** — `_query_issues` initialised `entries = []` and never populated it; only static `domains` list was returned. Added `_session["issue_log"]` list and `_log_issue(domain, message, context)` helper. Key error sites now call `_log_issue`: unit lookup failures (`unit_lookup`) and math engine exceptions (`combat_math`). `_query_issues` appends session log entries into the `entries` block with domain, note, and timestamp. | `engine.py` `_log_issue`, `_query_issues`, `_session` | 2026-04-02 |
| ✅ FIXED | HIGH | **`DEATHWING TERMINATOR SQUAD` absent from Dark Angels — misfiled in Space Marines dossier** — `spec deathwing terminator` and `list dark angels` both failed to surface the unit. Root cause: the parsed dossier parser placed DEATHWING TERMINATOR SQUAD (and also DEATHWING COMMAND SQUAD, DEATHWING KNIGHTS, DEATHWING STRIKEMASTER) into `space_marines_parsed_dossier.json` instead of `dark_angels_parsed_dossier.json`. Fixed by: (1) appending DEATHWING TERMINATOR SQUAD to `dark_angels_parsed_dossier.json`; (2) removing the four misfiled Dark Angels units from the Space Marines dossier (221 → 217 units). Note: in 10th edition there is no separate "Assault Terminators" datasheet for Dark Angels — DEATHWING TERMINATOR SQUAD covers all loadouts including thunder hammer + storm shield. | `dark_angels_parsed_dossier.json`, `space_marines_parsed_dossier.json` | 2026-04-02 |
| ✅ FIXED | MED | **`legend` command missing** — no glossary existed for the abbreviation columns (A, BS, WS, S, AP, D, T, Sv, W, Rng, OC, M, Ld), probability chain fields (Hit%, Wound%, Save%, Dmg, Kills), modifier flags (all 14 flags with math effects), or swinginess labels. Added `legend` / `key` / `abbrev` / `glossary` command returning `result_type: "legend"` with full structured data. Registered in `commands.py` (group: meta) and `parse_command`. | `engine.py` `_query_legend`, `commands.py` | 2026-04-02 |

---

### Determinism Gaps — `D0` items (stubs blocking real answers)

These are the gaps between what a command _accepts_ and what it actually _computes_.
Each row describes what full D2/D3 would look like.

| Priority | Command | Current state | What D2 looks like | Unlocks |
|----------|---------|---------------|---------------------|---------|
| ~~**HIGH**~~ ✅ FIXED | `<unit> vs <unit>` (combat) | ~~All math fields return `None`~~ → `combat_math_engine.py` copied in, `math_adapter.py` written to translate dossier format → full dice math: hit%, wound%, save%, expected_dmg, kill%, per-weapon breakdown. 2026-03-22 | — | — |
| ~~**HIGH**~~ ✅ FIXED | `<unit> vs <unit>` weapon table UI | ~~No weapon spec table in combat render~~ → Added `WeaponStatsTable` (A/BS/S/WR/AP/D) and `TargetingOutcome` (Hit%/Wound%/Kill%/Dmg bars) components. A column shows "2 (4)" squad-total format. BS and WR colour-coded green/red when modifier shifts them from baseline. 2026-03-22 | `WeaponStatsTable.jsx`, `TargetingOutcome.jsx`, `CombatBlock.jsx` | 2026-03-22 |
| ~~**HIGH**~~ ✅ FIXED | `rerun` command + live modifier toggles | ~~No way to toggle flags after a combat run~~ → Added `rerun` command (engine + registry), `ModifierToggles.jsx` inline toggle UI, `lastCombatIdRef` in-place result replacement in Terminal.jsx. Clicking `[ml]` / `[cover]` / `[lethal]` etc. fires `rerun --flag null` or `rerun --flag` and replaces the combat block in-place without appending a new entry. All-flags-off, unknown-flag, and rapid-toggle edge cases tested. 2026-03-22 | `engine.py`, `commands.py`, `ModifierToggles.jsx`, `CombatBlock.jsx`, `TerminalBlock.jsx`, `Terminal.jsx` | 2026-03-22 |
| ~~**HIGH**~~ ✅ FIXED | Monte Carlo loop never triggered | ~~`math_adapter.py` called only `compute_attack_result` (deterministic EV), never `monte_carlo_attack`~~ → `monte_carlo_attack` now called per-weapon at 5,000 trials (seed=42) after every combat query. `simulation_status: "ACTIVE"` when MC succeeds; `"OFFLINE"` if it errors. 2026-03-23 | `math_adapter.py`, `combat_math_engine.py` | 2026-03-23 |
| ~~**HIGH**~~ ✅ FIXED | Simulation Confidence always `null` / never shown | ~~`engine._query_combat` returned `"simulation": None`~~ → `math_adapter.compute_combat` now returns `simulation` block with `status`, `mode`, `iterations`, `error_margin`, `confidence`, `covers`. Engine passes it through. 2026-03-23 | `engine.py`, `math_adapter.py` | 2026-03-23 |
| ~~**HIGH**~~ ✅ FIXED | `SimulationConfidence.jsx` blank when MC offline | ~~Component returned `null` when `sim` was falsy — no fallback~~ → Component now renders an explicit `OFFLINE` banner ("Simulation Engine Offline: Showing Deterministic Averages") when `sim.status === "OFFLINE"` or `sim.mode === "deterministic"`. Interactive tooltip row explains Deterministic vs Monte Carlo math. 2026-03-23 | `SimulationConfidence.jsx` | 2026-03-23 |
| ~~**HIGH**~~ ✅ FIXED | `threat <faction>` + `analyze <unit>` counter picks | ~~Counters list always empty `[]`; threat level heuristic-only; no stat-based ranking~~ → Added `_compute_threat_score(attacker, defender)` (deterministic EV, no MC — calls `compute_attack_result` directly via math_adapter internals; fast enough for bulk unit pairs). Added `_compute_counters_math(enemy_unit, my_roster, top_n)`: for each roster unit computes bidirectional EV scores, normalises 0–100 by best dmg_dealt, returns `{name, score, reason, dmg_dealt, dmg_received, efficiency}`. `_query_analyze()` now uses `_compute_counters_math` when a roster is loaded (top 5); falls back to 10-unit faction sample if faction is set but no roster; falls back to `_suggest_counters` heuristics if no faction. `_query_threats()` pre-resolves the roster once then calls `_compute_counters_math(enemy_unit, resolved_roster, top_n=3)` for every threat unit — 51 Tyranid units with a 1-unit roster runs in 0.03 s. `show_counters: True` when roster is present, `False` when not. CounterBlock already rendered for any unit row that has counters in the threat_view. | `engine.py` `_compute_threat_score`, `_compute_counters_math`, `_query_analyze`, `_query_threats` | 2026-04-02 |
| ~~**MED**~~ ✅ FIXED | `strat <name>` | ~~Always returns stub~~ → `_build_stratagem_index()` runs at load time; indexes **1,046 stratagems** across 28 factions from `detachments[].stratagems[]`. Exact + shortest-substring match, detachment filter support. Frontend renders `stratagem_block` with WHEN/TARGET/EFFECT layout. 2026-03-30 | `loader.py` `_build_stratagem_index`, `TerminalBlock.jsx` `TerminalStratagemBlock` | 2026-03-30 |
| ~~**MED**~~ ✅ FIXED | `detachment <faction>` | ~~Always returns stub~~ → `_build_detachment_index()` runs at load time; indexes detachment name, rule name, full description, enhancements, and stratagems for every faction from `detachments[]` in parsed dossiers. `get_detachments(faction)` returns the full list by fuzzy faction match. Engine returns `result_type: "detachment_block"` with `{faction, detachments: [{name, rule_name, description, enhancements, stratagems}]}`. Frontend renders `TerminalDetachmentBlock` with strat/enhancement count badges; strat badge is clickable → injects `stratagem --detachment <name>`. Falls back to session faction if none specified. 2026-04-02 | `loader.py` `_build_detachment_index`, `engine.py` `_query_detachment`, `TerminalBlock.jsx` `TerminalDetachmentBlock` | 2026-04-02 |
| ~~**MED**~~ ✅ FIXED | `ability <name>` | ~~Stub unless the ability name happens to be in a unit's abilities list~~ → `_build_ability_index()` now runs at load time; indexes ~1,563 unique abilities across all factions. CORE/FACTION/CHARACTER entries indexed by their `summary` value. Returns `units[]` and `factions[]` in result. Frontend renders `ability_block` with clickable unit chips. 2026-03-30 | `loader.py`, `TerminalBlock.jsx` `TerminalAbilityBlock` | 2026-03-30 |
| ~~**MED**~~ ✅ FIXED | `enhancement <name>` | ~~Always returns stub~~ → `_build_enhancement_index()` runs at load time; indexes **637 enhancements** across 28 factions from `detachments[].enhancements[]`. Frontend renders `enhancement_block` with points cost badge and detachment tag. 2026-03-30 | `loader.py` `_build_enhancement_index`, `TerminalBlock.jsx` `TerminalEnhancementBlock` | 2026-03-30 |
| ~~**MED**~~ ✅ FIXED | `mission <name>` | ~~Always returns stub~~ → `_load_missions()` loads from `data/rules/missions.json`; **12 missions** (7 named primaries + 4 deployment maps + Annihilation). Frontend renders `mission_block` with scoring table, mission rules, and tip block. 2026-03-30 | `loader.py` `_load_missions`, `data/rules/missions.json`, `TerminalBlock.jsx` `TerminalMissionBlock` | 2026-03-30 |
| ~~**LOW**~~ ✅ FIXED | `analyze <unit>` | ~~Counters list empty `[]` when no roster loaded~~ → See threat/analyze fix above (2026-04-02). Counter picks now computed via real math with roster, faction sample, or heuristic fallback. | `engine.py` `_compute_counters_math` | 2026-04-02 |

---

### Data Gaps

Data that exists somewhere (old CT project or external sources) but isn't wired into the engine yet.

| Priority | Gap | Where the data lives | Notes |
|----------|-----|---------------------|-------|
| ~~HIGH~~ ✅ FIXED 2026-03-22 | Combat math engine | `combat_math_engine.py` + `math_adapter.py` | Adapted as module; full deterministic EV + Monte Carlo (5,000 trials). All 14 modifier flags wired. |
| ~~HIGH~~ ✅ FIXED 2026-03-30 | Stratagems | `12_factions/<faction>/*_parsed_dossier.json` → `stratagems[]` array | **1,046 stratagems indexed** at load time from all 28 factions |
| ~~HIGH~~ ✅ FIXED 2026-04-02 | Detachment rules | `12_factions/<faction>/*_parsed_dossier.json` → `detachments[]` array | `_build_detachment_index()` now indexes all detachments at load time. `detachment <faction>` command returns `detachment_block` with rule name, full text, strat/enhancement counts. Frontend renders `TerminalDetachmentBlock`. |
| ~~MED~~ ✅ FIXED 2026-03-30 | Enhancements | Same parsed dossiers → `enhancements[]` | **637 enhancements indexed** at load time |
| ~~MED~~ ✅ FIXED 2026-04-02 | Army rules | Same parsed dossiers → `army_rules` field | `_build_army_rules_index()` indexes per-faction army rules, filtering out Wahapedia scraping garbage. `army_rules <faction>` command returns `army_rules_block`. Frontend renders `TerminalArmyRulesBlock`. Data is partially populated across factions — some still have scraped nav text as the only entry, which is filtered out. |
| ~~MED~~ ✅ FIXED 2026-03-30 | Mission data | `data/rules/missions.json` | 12 missions loaded (7 primaries + 4 deployment maps + Annihilation). `mission` command fully implemented. |
| **HIGH** | Points values — quantified 2026-06-10: **null on 1,050/1,300 units (81%)**. Complete: BA/SW/EC; near-complete: BT 48/50, DA 51/57. SM megafile (216), tau (50/53) and the other 21 factions are 100% null | Dossiers `points` per unit | MFM v4.0 is the source; not yet extracted |
| **HIGH** | **Deathwatch dossier has only 2 units** (both legends) — other DW datasheets sit in the SM megafile (4 kill teams with empty unit_composition, Artemis, Cassius); Corvus Blackstar + Watch Master exist nowhere (2026-06-10 scan) | `data/factions/deathwatch/` | Rebuild from Agents/SM sources |
| **HIGH** | **Necrons: 4 detachments / 24 stratagems** — missing the codex core set (Awakened Dynasty, Canoptek Court, Hypercrypt Legion, Annihilation Legion, Obeisance Phalanx); lowest of any major faction (2026-06-10 scan) | `data/factions/necrons/` | — |
| **HIGH** | **SM megafile vs chapter files: 168 datasheets exist twice, 100% divergent** — chapter copies have points + `Inv` key but truncated weapon names ("Bolt", "Heavy bolt") and keyword leakage; megafile copies have full names but no points. Neither side authoritative. Overlaps: SW 49, BA 41, DA 41, BT 36, DW 1 (2026-06-10 scan — quantifies the open parser-doubling row above) | `data/factions/space marines/` + chapter dirs | Decide source of truth, merge best fields |
| MED | **Dark angels: 4 hand-stubbed units** with only name+stats (Interrogator-Chaplain, Deathwing Knights, Ravenwing Black Knights, Inner Circle Companions) — no weapons/abilities/composition/points (2026-06-10 scan) | `data/factions/dark angels/` | — |
| MED | **Legends/FW tagging incomplete** — tagged: tau 11+4FW, orks 16, tyranids 3, DW 2, DA 1; the other 24 factions are `legends:false` everywhere, 28/29 have no `forgeworld` key (e.g. Drukhari Tantalus, AM Aegis Defence Line plausibly untagged) (2026-06-10 scan) | all faction dossiers | — |
| MED | **65/299 detachments have placeholder names** `DETACHMENT (<rule>)`, all 0 enhancements; +2 garbage entries (AM detachment named in Nepali; DG `3+\t6"`) (2026-06-10 scan) | all faction dossiers | parser fix upstream |
| MED | **Army rules: 11 factions 0 entries** (loader's hardcoded descriptions paper over it); 11 of 18 populated contain Wahapedia nav garbage (2026-06-10 scan) | all faction dossiers | — |
| MED | **Leagues of Votann 12 units** — missing at least Hernkyn Yaegirs, Cthonian Earthshakers (2026-06-10 scan) | `data/factions/leagues of votann/` | — |
| LOW | **Keyword parse artifacts on 722/1,300 units** — `"KEYWORDS: "` prefix retained, last two tokens merged (`"Carnivores T'au Empire"`); 3 units with fully empty stats (Drukhari Incubi, GSC Jackal Alphus, Necron Lokhust Destroyers); ~20 torrent weapons with keywords spilled into the weapon name; stale `metadata.unit_count` in 5 dossiers (orks 85≠82, SM 221≠216, tau 46≠53, DA 56≠57, DG 32≠33); data typos `DEVASTATIG WOUNDS`/`ANTI-VECHILE` (2026-06-10 scan) | all faction dossiers | — |
| LOW | Active faction orchestrator files — **2026-06-10: confirmed nothing reads `main_faction/`; the tau dossier inside is stale (46–48 units vs 53, no Kroot/legends fixes) and ships to prod via `COPY data/`. Delete or archive, or re-point at the live dossier** | `data/combat_terminal/data/main_faction/tau/` | was: needed for strategy pipeline |
| ~~MED~~ ✅ FIXED 2026-04-02 | Wire VFS rosters → engine session | **Roster-sync architecture implemented.** Every command POST now includes a `roster_context: { my_units, opponent_units }` payload built by `parseRosterUnits()` (Terminal.jsx) from the active VFS rosters. `ExecBody` (server.py) accepts the optional field; the exec handler calls `engine.sync_roster_context()` before dispatching. `_query_combat()` overrides `att_models`/`def_models` from dossier minimums with actual roster squad sizes when a matching unit name is found in `_session["roster_my"]`/`_session["roster_enemy"]`. | `server.py` `ExecBody`, `engine.py` `sync_roster_context` + `_query_combat`, `App.jsx` `handleExec`, `Terminal.jsx` `parseRosterUnits` + `buildRosterContext` |
| LOW | Roster loading from disk | `data/combat_terminal/data/rosters/` | Pre-existing roster .txt files on disk are not yet imported into the VFS on first boot — could auto-import on startup |
| ~~HIGH~~ ✅ FIXED 2026-06-04 | Attached leader silently dropped when its datasheet is missing from the dossier | `engine.py` `_resolve_unit_dossier`, `_find_attached_leader`; `tau_parsed_dossier.json` | Kroot Trail Shaper was absent from the Tau dossier, so an attached leader resolved to `None` and was silently omitted from combat. Added the Kroot Trail Shaper datasheet; made leader resolution variant- and spacing-insensitive (`Trailshaper`/`Trail-Shaper` → `Kroot Trail Shaper`); made attachment matching nickname-aware so the leader merges only with the specific bodyguard instance; combat now emits a visible warning note when an attached leader has no datasheet. |
| ~~MED~~ ✅ FIXED 2026-06-04 | Kroot Hunting Pack characters missing from Tau dossier | `data/combat_terminal/data/factions/tau/tau_parsed_dossier.json` | Added Kroot War Shaper, Kroot Flesh Shaper, Kroot Lone-Spear (stats/weapons/abilities/points from Wahapedia 10e v1.2). Root cause: the dossier was built from a `codex_pdf_parse` (last_updated 2026-03-20) that predated / omitted the Kroot Hunting Pack character sheets. |
| LOW | Krootox Rampagers missing from Tau dossier | same | Current-codex "Other" Kroot unit still absent (datasheet fetch timed out during the 2026-06-04 pass). Add when convenient. |
| LOW | Diagnostic/`issues` panel does not detect missing unit datasheets | `engine.py` `_query_issues` | The panel only counts loaded items per *data domain* (stratagems, enhancements, missions, rules, abilities) — it never cross-references units against a complete published faction roster, so an absent datasheet is invisible. Missing units now surface reactively: a failed unit/leader lookup logs to the session issue log and combat shows a warning note. A full per-faction roster-completeness audit would need an external reference list to diff against. |

---

### Architecture Improvements — Math Mode

| Priority | Improvement | Why it matters |
|----------|-------------|----------------|
| ✅ DONE 2026-03-25 | **Math Mode — post-execution math ledger** — `math on` / `math off` toggle (Priority 7.2 in `Terminal.jsx`) activates post-execution math replay. After any command that returns `meta.math_ledger`, a `math_replay` stream entry is auto-injected. The replay is rendered by `MathModeBlock.jsx` with sequential playback controls (play/pause, step forward/back, jump to end, speed: 0.5×/1×/2×/4×). The ledger is built by `build_combat_ledger()` in `data/combat_terminal/math_ledger.py` — reconstructs the math narrative from already-computed combat results without touching `combat_math_engine.py`. Events are grouped by phase (DEFENDER PROFILE, WEAPON — <name>, MONTE CARLO SIMULATION). High-importance events highlighted in amber. `mathLedger.js` provides the same API for any future client-side simulation code. `mathmode` command registered in `commands.py` (aliases: `math`). | `math_ledger.py`, `mathLedger.js`, `MathModeBlock.jsx`, `Terminal.jsx`, `TerminalBlock.jsx`, `engine.py`, `commands.py` | 2026-03-25 |

### Architecture Improvements

> **Session note 2026-06-05 — gating decisions + what's still open.**
> P0-2 gate answered **multi-user/multi-tab = YES**, so per-client session isolation was built (see Bugs table). Still **OPEN** after this session:
> - **P2 god-objects / contract leak** (deferred, needs a dedicated session): `engine.py` ~4.2k-line god-object (`_query_combat` ~815 lines); `Terminal.jsx` ~1.67k-line god-component with a duplicated router; `EngineBase` contract leak (web layer calls `engine.exec()`/`sync_roster_context()`/`set_session()` and reaches into `engine._loader._units` — none in the contract, so config.json swappability is still false); `schema()` advertises `supports_cli=False` commands the dispatcher rejects.
> - **Residual P1 (by design / low risk):** shared-roster ownership is enforced only when a `requester` is supplied — a hand-crafted request omitting it can still delete an owned roster (acceptable for the trusted-friends deploy; full per-account auth was NOT added since exposure wasn't escalated). `finalize_battle` is still synchronous `def` (blocks a worker for seconds) — made idempotent rather than async. `term_aliases` store is still uncapped (per-keystroke regex compile is a latent DoS).
> - **Laptop TODO (sandbox can't commit):** `git rm --cached render/web/vite.config.js.timestamp-*.mjs` (22 tracked-but-gitignored Vite temp files) — fold into the commit below.

| Priority | Improvement | Why it matters |
|----------|-------------|----------------|
| ✅ DONE 2026-06-05 | **Repo hygiene (P3)** — (1) deleted `Dockerfile.new` (stale broken duplicate missing the three store `COPY` lines whose absence caused the documented prod 502); (2) removed the two 0-byte junk rosters (`mine_delet2`, `mine_delete`) and four clearly example/test enemy rosters (`enemy_dark-angels-test*`, `enemy_terminator-example-list`, `enemy_tyranids-example-roster`) from `data/.../rosters/` — these ship to prod via `COPY data/ data/`; kept all personal `mine_*` lists; (3) pinned `requirements.txt` with upper bounds so a breaking upstream major can't auto-install (pyairtable capped `<3.0` since the Airtable store code targets the 2.x API that 3.x broke). Still TODO on the laptop: `git rm --cached render/web/vite.config.js.timestamp-*.mjs` (22 tracked-but-gitignored Vite temp files — sandbox git index isn't committed from here). | `Dockerfile.new`, `data/.../rosters/`, `requirements.txt` | 2026-06-05 |
| ✅ DONE 2026-06-05 | **Test coverage for the two biggest untested modules (P3)** — coverage was pure-function-only (math + crusade helpers, hand-built fixtures); `loader.py` (real-dossier parsing) and the 33 FastAPI routes had zero tests, so a real-data parsing regression passed every test but broke prod. Added `tests/test_loader_smoke.py` (6 tests: loads ALL factions for real, asserts the corpus is substantial — 29 factions / 1299 units / 1046 strats / 637 enhancements / 1594 abilities — every faction has units, two known units across factions resolve with correct stat+weapon shape, and a bogus name returns `None` not a fabricated stub) and `tests/test_api_integration.py` (10 tests via Starlette `TestClient`: engine list/schema/commands, the query + exec routes run a real command/combat end-to-end, the exec roster_context wiring, unknown-engine 404, health/version, and that the Airtable-backed reports route degrades to a 503 JSON rather than a 500 stacktrace). Both files run under `python3 tests/<file>.py` (self-runner) or pytest. | `tests/test_loader_smoke.py`, `tests/test_api_integration.py` | 2026-06-05 |
| ✅ DONE 2026-06-04 | **Stat-modifier hover popover (BS / WR / AP)** — coloured stat cells in the combat weapon table now explain *why* they shifted. `engine._query_combat` attaches `bs_reason`/`wr_reason`/`ap_reason` objects per weapon (`{stat, from, to, delta, via, direction, text}`) built by `_collect_stat_sources()`/`_collect_ap_sources()` + `_build_stat_reason()`/`_build_ap_reason()`, attributing the delta to the active modifier flags that touched the to-Hit / to-Wound roll or the AP characteristic (ml→Markerlight, hitplus/wndplus, lance, stealth, indirect, torrent, heavy [keyword-gated], applus/apminus). Frontend `WeaponStatsTable.jsx` `StatCell` renders a Radix `Tooltip` popover (dotted-underline affordance, green/red border by direction) showing e.g. "BS 4+ → 3+ / +1 to Hit · via Markerlight" or "AP -3 → -4 / +1 AP". Cells with no modifier reason stay plain. | `engine.py`, `WeaponStatsTable.jsx` | 2026-06-04 |
| ✅ DONE 2026-06-04 | **AP modifier flags wired into math** — `--eap` / `--eap2` / `--eap3` improve a weapon's Armour Penetration by N (attacker buff: AP-1 → AP-2; the "improve the Armour Penetration characteristic by 1" abilities/stratagems); `--eapdef` / `--eapdef2` worsen incoming AP (defender ability: AP-2 → AP-1, e.g. T'au Commander in Enforcer Battlesuit "worsen the Armour Penetration characteristic of that attack by 1" — placed after `vs`). `_apply_flags` maps both to the existing `ap_modifier` field (eap → `ap_modifier -= N`, eapdef → `ap_modifier += N`; convention: `effective_ap = weapon.ap − ap_modifier`; eapdef checked before eap since it shares the prefix). `compute_save_target` now floors `effective_ap` at 0 so worsened AP can cancel penetration but never grants a sub-armour bonus save. Flags added to `KNOWN_FLAG_BASES`, `FLAG_NOTE_MAP` (WarningBlock notes), `legend` docs, and the AP-cell popover (`_collect_ap_sources`). Frontend `ModifierToggles` marks `eapdef` as a defensive (red) token. Verified end-to-end: eap raises EV (2.96→3.70→4.44), eapdef lowers it (2.22→1.48), AP cell recolours + popover ("+1 AP" / "-1 AP via target"); 69/69 math tests green. | `math_adapter.py`, `combat_math_engine.py`, `engine.py`, `WeaponStatsTable.jsx`, `ModifierToggles.jsx` | 2026-06-04 |
| ✅ DONE 2026-06-04 | **Colon-free flag syntax — `--sus` / `--invuln4`** — parametrised flags standardised on embedded-number form (no colons). Sustained Hits is now `--sus` / `--sus1` / `--sus2` / `--sus3` (`_apply_flags` `key.startswith("sus")`, strips `sus`/`sustained` prefix; legacy `--sustained[N]` still parses). Invuln override is `--invuln4` / `--invuln5` / `--invuln6` (embedded already worked; legend display updated from `[invuln:4]` to `[invuln4]`). Crusade trait flags in `crusadeTraits.js` converted to embedded form (`hitplus1`, `fnp6`, `wndplus1`, `sus1`, `invuln5`, `hitplus-1`). Reason: see colon-parse bug below — embedded forms are reliable on both sides of `vs`. | `math_adapter.py`, `engine.py`, `crusadeTraits.js` | 2026-06-04 |
| ✅ DONE 2026-03-24 | **Terminal-integrated file upload system** — `upload roster`, `upload enemy roster`, `upload campaign` commands. Each emits an inline `upload_block` result rendered by `UploadBlock.jsx` wrapping the `Pattern` component. File handler calls `processUpload(content, filename)`. Paste fallback at Priority 8.55 in submit(). Pipeline: validate ext → `upload_roster` flow at faction step → name step → save. Enemy flag auto-assigns enemy roster. Campaign tries JSON name auto-detect then prompts. New files: `use-file-upload.js`, `UploadBlock.jsx`. Updated: `file-upload.jsx`, `TerminalBlock.jsx`, `Terminal.jsx`. | `src/hooks/use-file-upload.js`, `UploadBlock.jsx`, `file-upload.jsx`, `TerminalBlock.jsx`, `Terminal.jsx` | 2026-03-24 |
| ✅ DONE 2026-03-24 | **Theme Directory + Interactive Selection** — `themes` / `theme list` command (Priority 7.1) renders a full numbered directory: `[✓]` for unlocked, `[ ]` for locked, with activation hints and attempt prompts. Arms a `themes_select` client flow — typing a number (1–3) or a theme name targets the exact challenge; already-unlocked entries report status instead of re-challenging. `theme <locked-id>` now returns `[ ERROR ] : Visual override not authorized. Solve the challenge first.` Unlock success message updated to `[ SUCCESS ] : <ID> is now available. Type 'theme <id>' to activate.` All wired via `THEME_REGISTRY`, `getChallengeForTheme`, and the existing `unlock_theme` flow. `themes` registered in `commands.py`. | `Terminal.jsx`, `commands.py` | 2026-03-24 |
| ✅ DONE 2026-03-24 | **Unlock Theme system** — Scalable logic-gate system for hidden UI themes. `src/data/themeRegistry.js` maps theme IDs to metadata; `src/data/challengeRegistry.js` holds riddle objects (text, answer, rewardThemeId). `Terminal.jsx` Priority 7 `theme` command updated to read base + unlocked themes via localStorage (`ct_unlocked_themes`). Priority 7.5 `unlock theme` initiates a challenge flow; `handleFlowStep` type `unlock_theme` validates the answer, persists the unlock, and applies the theme instantly via `onTheme`. App.jsx initialises from `ct_active_theme` localStorage key on mount and persists on every theme change. CSS: `[data-theme="mainframe"]` block in `index.css` — pure black, #33ff33, forced monospace 14px, no bold, no box-shadows, no gradients, no border-radius. Adding a new unlockable theme requires: 1 entry in themeRegistry.js + 1 entry in challengeRegistry.js + 1 CSS block in index.css. | `src/data/themeRegistry.js`, `src/data/challengeRegistry.js`, `Terminal.jsx`, `App.jsx`, `index.css`, `commands.py` | 2026-03-24 |
| ✅ DONE 2026-03-23 | **VFS + Roster/Campaign persistence system** — `vfs.js` (localStorage VFS), `RosterListBlock.jsx`, `RosterActionBlock.jsx`, `RoleAssignBlock.jsx`, `CampaignListBlock.jsx`, `RosterUploadBlock.jsx`. Commands: `rosters`, `load roster`, `set roster player/enemy`, `upload roster`, `rename roster`, `edit roster`, `delete roster`, `campaigns`, `load campaign`, `new campaign`. Multi-step client flow handler in `Terminal.jsx` (Priority 8.6–8.8). Drag-and-drop .txt file to upload roster. All operations are localStorage-only; engine session not wired yet (see Data Gaps). | `lib/vfs.js`, `components/roster/*`, `Terminal.jsx`, `commands.py` |
| ✅ DONE 2026-03-23 | **Engine Status Dashboard (DIAG page)** — New `DiagnosticsPage.jsx` (accessible via `diag` command or DIAG nav tab). Contains: System Health Header (MC/Deterministic badges + animated SVG heartbeat monitor), Animated Data Flow Pipeline diagram (5 nodes with CSS stroke-dash flow animation, node turns red on failure), Data Integrity Grid (per-faction completion bars with traffic-light colours — green ≥90%, amber 70–89%, red <70%, plus Missing Data Table for critical-gap factions), Engine Docs (interactive accordion explaining Deterministic, Monte Carlo, AP, Swinginess, Confidence, Pipeline). Polls `/api/engines/{id}/status` every 4s. `engine.status()` now returns `simulation_mode`, `simulation_config`, `pipeline`, and `per_faction` (from enhanced `loader.status()`). | App.jsx, DiagnosticsPage.jsx, engine.py, loader.py, Terminal.jsx |
| ✅ DONE | Terminal-first architecture — all clicks anywhere in the UI (UnitsView rows, RulesView lookups, TerminalBlock links, disambiguation numbers) resolve to a visible terminal command via animated inject into the CommandBar (`onInject` / `animateAndSubmit`) rather than rendering directly | 2026-03-22 |
| ✅ DONE | Disambiguation flow — ambiguous `spec`/`combat`/`analyze` queries return `result_type: "disambiguation"` with a numbered match list; bare integer input (`"2"`) resolves the pending context via `_select` handler; frontend renders `DisambiguationBlock` | 2026-03-22 |
| ✅ DONE | **Navigation command system** — full priority-ordered submit() in `Terminal.jsx`: `!!` repeat, `clear`, `delete` (output stack undo), `back`/`forward` (nav history stack via `navStackRef`), page nav tokens (`home`/`units`/`rules`/`campaign`/`demo` + single-letter aliases). Nav bar clicks now animate-inject the command instead of directly switching views. `onNavigate` prop wired from App → Terminal → `setActiveView`. All nav commands registered in `commands.py` with `supports_cli=False` and `group="navigation"` for tab completion + help. `result_type: "system_msg"` added to `TerminalBlock` for dim italic feedback lines. | 2026-03-22 |
| ✅ DONE 2026-03-30 | **Rule hierarchy graph** — `_load_rules()` now also reads `rule_index.json` and builds `self._rule_related` mapping each rule name → list of peer rule names. Mechanic-group peers are primary source (e.g. `lethal hits → heavy, assault, sustained hits, twin-linked`); for isolated rules, a 2-tag overlap fallback provides weapon-ability peers (e.g. `devastating wounds → rapid fire, pistol, torrent, blast, heavy`). `get_rule()` returns `related[]` injected into result. Frontend `TerminalRuleBlock` already renders clickable See Also chips. | `loader.py` `_load_rules`, `rule_index.json` | 2026-03-30 |
| ✅ DONE 2026-03-30 | **`issues` command** — New meta command (`issues` / `stubs` / `missing`) runs a domain-by-domain data audit: stratagems, enhancements, missions, rules, abilities, threat scoring, counter picks. Returns `result_type: "stub_log"` rendered by new `IssuesLog` component showing D0/D1/D2 level, domain, and status note. Registered in `commands.py` and `engine.py` dispatch. | `engine.py` `_query_issues`, `commands.py`, `TerminalBlock.jsx` `IssuesLog` | 2026-03-30 |
| ✅ DONE 2026-03-30 | **stratagem / enhancement / mission / ability frontend renderers** — All four `result_type` values were falling through to raw JSON dump. Added `TerminalStratagemBlock` (WHEN/TARGET/EFFECT table, CP badge, detachment tag), `TerminalEnhancementBlock` (pts badge, rules text, applies-to), `TerminalMissionBlock` (scoring rows, mission rules, tip block), `TerminalAbilityBlock` (type_tag badge, units as clickable `spec <unit>` chips). | `TerminalBlock.jsx` | 2026-03-30 |
| ✅ FIXED 2026-04-02 | **`onInject` threading — SpecBlock, ThreatCard, ThreatBlock, CounterBlock** — Unit name headers and counter-pick names were plain text or used `onSubmit` (silent submit). Now: `SpecBannerCard` title is clickable → `spec <unit>`; `ThreatBanner` unit name is clickable → `spec <unit>`; `ThreatCardRow` collapsed-header unit name is a separate clickable span (stopPropagation so row toggle still works independently) → `spec <unit>`; `CounterBlock` counter names use `onInject` (animate-type) preferentially over `onSubmit` for the richer inject-and-submit UX. `onInject` prop threaded through: `SpecBlock` → `SpecBannerCard`; `ThreatCard` → `ThreatBanner` + `CounterBlock`; `ThreatBlock` → `ThreatCardRow` → `CollapsedHeader` + inner `ThreatCard`. | `spec/SpecBannerCard.jsx`, `SpecBlock.jsx`, `threat/ThreatBanner.jsx`, `threat/CounterBlock.jsx`, `ThreatCard.jsx`, `threat/ThreatCardRow.jsx`, `ThreatBlock.jsx` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **Weapon toggle state reset on rerun** — `disabledWeapons` `useState` in `CombatBlock` was vulnerable to being reset whenever React decided to remount the component (e.g. conditional render path changes during a rerun cycle). Fixed by keying `CombatBlock` in `TerminalBlock.jsx` on `attacker_name||defender_name`: same attacker/defender pair reuses the exact same component instance (and its toggle state) across reruns; a genuinely different combat matchup gets a fresh instance with clean state. | `TerminalBlock.jsx` (`combat` case) | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **`nextturn` returned plain text** — `_query_nextturn()` returned `result_type: "text"` with a single line. Upgraded to `result_type: "nextturn_block"` returning: `turn` (new round number), `cp_gained` (1 CP, matched play standard), `phase_reminders` (turn-sensitive notes: CP gain, secondary scoring from T2, primary scoring from T3, penultimate/final round warnings, over-5-rounds alert), `active_modifiers` (flags from `_session["last_combat"]` for recap), `attacker`/`defender` (last combat pair with re-run link), `faction`/`enemy_faction` (clickable sidebar with list/threat shortcuts). `TerminalNextTurnBlock` component added to `TerminalBlock.jsx` renders the full layout. `nextturn_block` case wired into the result dispatcher. | `engine.py` `_query_nextturn`, `TerminalBlock.jsx` `TerminalNextTurnBlock` | 2026-04-02 |
| ~~LOW~~ ✅ FIXED 2026-04-02 | **CLI `supports_cli` filtering** — `render/cli/terminal.py` tab completion and `help` now filter to only CLI-visible commands using the `supports_cli` flag from schema. Navigation group skipped entirely. Help grouped by `GROUP_ORDER` / `GROUP_LABELS` from `commands.py`. Web-only commands (VFS, roster upload, themes, navigation) no longer appear in the CLI. | `render/cli/terminal.py` `_repl`, `_print_help` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **CLI `history` command** — `result_type: "history"` was a stub returning `data: None`; CLI now intercepts this result type in `_repl` and prints the in-session command list from `self._history`. Each entry numbered. `history` is recorded after every command (before dispatch), so it always includes the current query. | `render/cli/terminal.py` `_render_history` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **CLI math mode prefix** — `math on` / `math off` permanently toggles a `math_mode` flag in `_repl`. Prefixing any command with `math ` (e.g. `math broadside vs intercessors`) runs it with a one-shot `math_ledger` replay printed immediately after the result. The ledger renderer (`_render_math_ledger`) handles the nested group→event structure, printing group headers and `▶`-marking high-importance events. Mirrors the web UI's Math Mode logic in `Terminal.jsx`. | `render/cli/terminal.py` `_repl`, `_render_math_ledger` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **`dice` roller implemented** — `_query_dice()` in `engine.py` was a stub. Now parses `NdN`, `NdN+M`, `NdN-M` expressions with optional `reroll N` and `explode` modifiers. Returns `result_type: "text"` showing individual rolls, modifier, and total. Supports 1–100 dice, 2–1000 faces. `stub=True` removed from `dice` command in `commands.py`. | `engine.py` `_query_dice`, `commands.py` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **CLI alias resolution** — CLI `_repl` now resolves aliases via `_cmds.resolve()` before passing the token to `engine.query()`. Previously `roll`, `vs`, `hist`, etc. returned "Unknown command" in the CLI; now they correctly map to `dice`, `combat`, `history`. | `render/cli/terminal.py` `_repl` | 2026-04-02 |
| ✅ DONE 2026-06-02 | **Rosters tab replaces Campaign tab** — New `RostersContext.jsx` dedicated panel (no terminal) showing session state dashboard, player/enemy army panels with inline load/change actions, saved rosters grouped by faction with [P]/[E]/[✎]/[×] quick actions, and campaigns section. Campaign tab removed; `campaign` and `c` now route to `rosters`. Stale "coming soon" messages cleaned up across ModifierImpact, engine.py roster text, DemoView dice stub, ThreatSummary placeholder labels. Session summary `target`/`mods`/`campaign` null fields removed from engine output and TerminalSessionBlock display. | `RostersContext.jsx`, `App.jsx`, `Terminal.jsx`, `commands.py`, `engine.py`, `TerminalBlock.jsx`, `ModifierImpact.jsx`, `DemoView.jsx`, `ThreatSummary.jsx` |
| ✅ DONE 2026-06-02 | **UI Polish — BUILD_PLAN Phase 1-3 (19 items)** — Theme switcher dropdown (◐ icon, Dark/Light/Console). History dropdown cleanup (420px, sticky header, 32px rows). Output separators (1px, opacity 0.4). Collapse ▸/▾ + delete ✕ on every entry. Edit button always visible as `[✎ EDIT]` chip. Tab autocomplete dropdown (12 matches, arrow nav, red flash). Star icon 22px gold with glow. Progress bars shrunk to 5px. Terminal-style `─── TITLE ───` module separators. Semantic colors (Hit%=green, Wound%=yellow, Failsave%=red, Kills=amber, Kill Chance=red). Modifier toggle scroll-jump fixed. Unknown modifier suggestions via Levenshtein. `clear roster player/enemy` command. Rosters page condensed to expandable faction list. | `App.jsx`, `Terminal.jsx`, `TerminalBlock.jsx`, `CombatBlock.jsx`, `TargetingOutcome.jsx`, `SpecBannerCard.jsx`, `RostersContext.jsx`, `colors.js`, `math_adapter.py`, `engine.py` |
| ~~MED~~ ✅ DONE 2026-06-02 | ~~Lazy faction loading~~ | Refactored loader: `_discover_factions()` records paths without parsing (17ms), `_load_faction()` parses dossier once on first access (consolidates 5 separate passes into 1). Boot time 380ms → 17ms (22x faster). All data counts preserved: 1298 units, 1046 stratagems, 637 enhancements, 1561 abilities. |
| ~~MED~~ ✅ DONE 2026-03-30 | ~~Ability index built at load time from all `unit.abilities[]` arrays~~ | Done — 1,563 abilities indexed |
| ~~MED~~ ✅ DONE 2026-03-30 | ~~Stratagem + detachment index built at load time from dossier `stratagems[]` / `detachments[]`~~ | Done — 1,046 stratagems + 637 enhancements indexed |
| ~~MED~~ ✅ DONE | ~~Add `models` field to roster session~~ | Already implemented: `parseRosterUnits()` in Terminal.jsx extracts `{name, faction, models}` from roster text (e.g. "3x Unit Name"). `buildRosterContext()` sends to engine via `sync_roster_context()`. `_query_combat()` overrides `att_models`/`def_models` from roster `models` field. |
| LOW | Normalise faction key at load time (lowercase, underscores) so lookup never depends on filesystem naming | Prevents silent mismatches when faction folder name ≠ dossier filename prefix |
| **HIGH** | **`threat <faction>` is O(enemy×roster×2×weapons), uncached** — 216-unit SM dossier × 10-unit roster ≈ 24k `compute_attack_result` calls per command; `_unit_to_target` rebuilt per pair (4,320× vs ~226×); imports inside the hot loop (engine.py:4183). Memoize conversions, hoist imports. (2026-06-10 scan) | `engine.py` `_query_threats`, `_compute_counters_math` |
| **HIGH** | **Airtable boilerplate quadruplicated** (profiles/shared_rosters/reports/crusade_store, ~90 dup lines, `_slugify` ×4) — drift already real: `reports.py:113` escapes only quotes, others also escape backslashes (the injection defence). Every op builds a fresh `Api(token)` → new TLS handshake per call. One `airtable_common.py` with cached Api. (2026-06-10 scan) | `render/web/*.py` |
| MED | **Frontend dead weight** — ~28/31 shadcn `ui/` components unreachable (only card+tooltip used), dragging 37MB lucide-react + 10 unused radix deps; 8 dead top-level components (DemoView, HabitTracker, chart-radar-default, ActionCard, StatCard, QueryPanel, ResultView, EngineStatus); `switch-block.jsx` imports nonexistent files. Delete + prune package.json. (2026-06-10 scan) | `render/web/src/components/` |
| MED | **`dist/` serves the March 25 vite bundle** — June 5 esbuild output orphaned as `assets/main.js`; two parallel build pipelines (vite.config.js vs build.mjs), dead `recharts-mock.js` alias, GNU-only sed in `build:esbuild`. Pick one pipeline, rebuild. (2026-06-10 scan) | `render/web/dist/`, `build.mjs`, `vite.config.js` |
| MED | **`loader.status()` calls `_ensure_all_loaded()`** — first DIAG poll force-parses all 29 dossiers, defeating the 22× lazy-load win. Report discovered-path counts for unloaded factions instead. (2026-06-10 scan) | `loader.py:658` |
| MED | **Per-client session store unbounded** — no cap/TTL on `_sessions`; memory grows per browser tab forever in the long-running container. LRU-cap ~100 or TTL-sweep. (2026-06-10 scan) | `engine.py:479` |
| MED | **Dockerfile `npm install` without lockfile** — `package-lock.json` not COPYed → nondeterministic prod builds. `COPY package-lock.json` + `npm ci`. (2026-06-10 scan) | `Dockerfile` |
| LOW | 2026-06-10 hygiene cluster: consumed CT2_* session prompts + 2 .docx tracked at root; `data/.../rosters/` (186KB) read by no code but shipped to prod; 0-byte `ct_config.json` referenced by nothing; ~95 `tmp_obj_*` junk files in `.git/objects` (`git gc --prune=now`); `_query_combat` repeats the disambiguate-or-pick block ~6×; TerminalBlock.jsx now 2,524 lines; rank/XP mirror (crusade_store↔crusade.js) in sync but unguarded — add one cross-check test | repo root, `data/`, `.git`, `engine.py`, `TerminalBlock.jsx` |
| ~~LOW~~ ✅ DONE 2026-03-30 | ~~`status()` should report per-command determinism level~~ | Done via `issues` command — domain-by-domain D0/D1/D2 report. |
| ✅ DONE 2026-04-02 | **Test suite** — `tests/test_combat_math.py`: 69 automated tests covering baseline EV math, all 14 modifier flag types, modifier math effects, multi-model attack scaling, weapon keyword parsing, end-to-end flag→math integration, Monte Carlo sanity, sensitivity sweep, and edge cases. Run standalone (`python tests/test_combat_math.py`) or via pytest. No external dependencies beyond stdlib. | `tests/test_combat_math.py` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **`legend` frontend renderer** — `TerminalLegendBlock` added to `TerminalBlock.jsx`. Tabbed UI: Stat Columns, Probability Chain, Modifier Flags, Swinginess Labels. Notes footer always visible. `case "legend":` wired into the result dispatcher. | `TerminalBlock.jsx` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **Blast minimum-3 attacks** — `def_models` param threaded from `engine.py` → `compute_combat()` → `_run_ev()` / `_run_mc()` / `_run_total()`. BLAST min-3 applied before squad scaling when `use_blast=True` and `def_models >= 6`. `_parse_min_models()` used on defender `unit_composition`. | `math_adapter.py`, `engine.py` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **Rapid Fire conditional application** — Added `rf_value: float = 0.0` field to `AttackModifiers`. `_weapon_to_profile` stores RAPID FIRE N in `rf_value` instead of `extra_attacks`. `_run_ev` / `_run_mc` / `_run_total` apply `merged.extra_attacks += merged.rf_value` only when `merged.use_rapid_fire=True` (i.e. `--rf` flag present). | `combat_math_engine.py`, `math_adapter.py` | 2026-04-02 |
| ✅ FIXED 2026-04-02 | **Kill% semantic** — `kill_pct` in engine.py was sourced from `fail_save_pct` (P(unsaved wound per attack in chain)), not a true kill probability. Added `_kill_chance_from_mc()` helper to `math_adapter.py` computing P(≥1 kill) from MC `kill_bucket_probabilities["0"]`. New `kill_chance_pct` field in per_weapon output; `engine.py` now sets `w["kill_pct"] = pw.get("kill_chance_pct")` and separately stores `w["fail_save_pct"]`. | `math_adapter.py`, `engine.py` | 2026-04-02 |

---

## Crusade Tracker (render/web — CRUSADE tab)

Server-backed Crusade campaign manager (Airtable `CrusadeCampaigns` + `CrusadeUnits`).
Not a data engine — lives in the render layer alongside the rosters system.

### Done

| Status | Session | Description | Component |
|--------|---------|-------------|-----------|
| ✅ DONE 2026-06-04 | S2 | **Editable Order of Battle** — `OrderOfBattle.jsx`: add unit (manual form), remove unit, live supply gauge (used pts vs supply limit, over-limit highlighted red), click-to-expand unit rows. Wired into `CrusadeContext` `CampaignDetail`, replacing the read-only OOB summary. `reloadActive()` re-fetches the campaign after every edit. | `crusade/OrderOfBattle.jsx`, `CrusadeContext.jsx` |
| ✅ DONE 2026-06-04 | S2 | **Roster-text import** — `ImportFlow` parses pasted CT roster text with the shared `parseRosterUnits`, previews parsed units (flagging duplicates), then bulk-creates units. Default mode: append + skip duplicates (name|nickname match). `parseRosterUnits` extracted from `Terminal.jsx` into `lib/rosterParse.js` (single source of truth; Terminal now imports it). | `lib/rosterParse.js`, `crusade/OrderOfBattle.jsx`, `lib/crusade.js` `importUnits` |
| ✅ DONE 2026-06-04 | S2 | **CrusadeCard** — per-unit detail editor: nickname, points, models, leader toggle, attachment (dropdown of other OOB units), XP with auto-derived rank (`rankForXp` per spec §3.1) + manual override, loadout add/remove chips, free-form battle honours + scars ({name, effect}), marked-for-greatness toggle. Read-only counters: battles fought/survived, kills, crusade points. Saves via `updateUnit`. | `crusade/CrusadeCard.jsx`, `crusade/ui.jsx`, `lib/crusade.js` rank helpers |
| ✅ DONE 2026-06-04 | S3 | **Muster + combat bridge** — `MusterPanel` (checkbox OOB selector, point-limit gauge, mark-for-greatness, Start Battle). `startBattle()` posts `roster_context` of crusade-enriched units + a `faction <slug>` command to the engine exec endpoint, so terminal combat/spec commands immediately use the crusade data. Engine: `_crusade_combat_mods()` + `_crusade_block_for()`; `_query_combat` merges honour/scar `flag`s into `math_flags` (auto-applied to compute/sensitivity/ledger) and adds ◈ notes + amber/red abilities; `_build_spec_result` adds the rank badge to the subtitle and honours/scars to abilities. Verified: +1-hit honour raised EV 14.3→19.09; full 69-test math suite still green. | `crusade/MusterPanel.jsx`, `lib/crusade.js` (`buildCrusadeUnit`/`startBattle`), `engine.py`, `App.jsx`, `CrusadeContext.jsx` |
| ✅ DONE 2026-06-04 | S3 | **hitplus/wndplus math flags** — new `_apply_flags` tokens `hitplus[:N]` / `wndplus[:N]` set `hit_bonus`/`wound_bonus` (supports negatives, e.g. the Disgraced scar `hitplus:-1`); added to `KNOWN_FLAG_BASES` and engine `FLAG_NOTE_MAP`. Gives the most common Crusade battle traits (+1 to Hit/Wound) a real math effect. | `math_adapter.py`, `engine.py` |
| ✅ DONE 2026-06-04 | S3 | **Starter trait tables + flag picker** — `lib/crusadeTraits.js` (infantry/vehicle battle traits, generic scars, honour extras) mapped to engine flags. CrusadeCard honour/scar editor gains a quick-pick dropdown + optional manual `flag` field; flagged entries show a cyan ◈ token. | `lib/crusadeTraits.js`, `crusade/CrusadeCard.jsx` |
| ✅ DONE 2026-06-04 | S4 | **CrusadeBattles table + finalize transaction** — added `BATTLES_TABLE` to `TABLE_SPECS` (self-provisions); `create_battle`/`list_battles` CRUD mirroring the unit helpers (UnitResults = JSON array). New pure helpers `rank_for_xp`, `compute_auto_xp` (§3.2), `promotion_slots` (§3.5). `finalize_battle()` = the atomic post-battle transaction: per-unit XP+rank+honours/scars+counters via `update_unit`, writes one battle row, bumps campaign RP/W-L-D/battle_count, clears `state.active_battle`. All in `crusade_store.py` (already in the Dockerfile allowlist → no new COPY needed). | `crusade_store.py` |
| ✅ DONE 2026-06-04 | S4 | **Battle API routes + JS client** — `GET/POST /api/crusade/campaigns/{id}/battles` (+ `BattleFinalizeBody`). `lib/crusade.js`: `listBattles`, `finalizeBattle`, `buildActiveBattle`/`persistActiveBattle` (in-progress state lives in campaign `State.active_battle`), `autoXp`/`rankIndex`/`isHeroicPlus`/`promotionSlots` mirroring the server math. | `server.py`, `lib/crusade.js` |
| ✅ DONE 2026-06-04 | S4 | **BattleTracker** — in-battle per-unit kill steppers + destroyed toggle over `state.active_battle.units`; live counters in React, debounce-persisted (~1.5s) to campaign State so a refresh restores. Re-muster (re-pushes the in-memory engine roster), Abandon, End Battle → PostBattleFlow. MusterPanel now captures a mission + persists `active_battle` on Start Battle, then reloads (parent swaps to the tracker). | `crusade/BattleTracker.jsx`, `crusade/MusterPanel.jsx` |
| ✅ DONE 2026-06-04 | S4 | **PostBattleFlow + HonourPicker** — result picker; auto XP per §3.2 with per-unit manual override (↺ reset); **manual** scar selection on destroyed units (scope note below — no auto D6); promotion detection via `promotionSlots`; `HonourPicker` slots `n` battle traits/relics per rank-up (reuses `crusadeTraits.js` incl. combat flags); +1 RP (editable) + notes; Finalize calls the server transaction and reloads. | `crusade/PostBattleFlow.jsx`, `crusade/HonourPicker.jsx` |
| ✅ DONE 2026-06-04 | S4 | **RP actions + battle history + wiring** — `RPActions` (§3.4: Fresh Recruits, Rearm/Resupply, Repair/Recuperate, Increase Supply Limit, Heroic Relic — each decrements `rp` and mutates the unit/campaign, disabled when RP < cost). `BattleHistory` lists CrusadeBattles with an expandable per-unit breakdown. `CrusadeContext.CampaignDetail` routes active-battle → tracker/post-battle, else muster + RP actions + history. Python tests for XP/promotion logic (`tests/test_crusade_logic.py`, 16 green); combat-math suite still 69 green; frontend builds via `build.mjs`. | `crusade/RPActions.jsx`, `crusade/BattleHistory.jsx`, `CrusadeContext.jsx`, `tests/test_crusade_logic.py` |
| ✅ DONE 2026-06-04 | S5 | **Rules data + defensive math + defender-side bridge** — completed the Crusade rules layer. (1) New defensive math flags in `math_adapter` (`woundsplus`, `dmgreduce`/`dmgred`, `svplus`, `svminus`) with `_suffix_int` parser, added to `KNOWN_FLAG_BASES`, the `legend`, and combat flag-note callouts. (2) `is_defensive_flag` + `DEFENSIVE_FLAG_BASES` classify every flag; `_crusade_combat_mods(entry, side)` routes attacker→offensive and defender→defensive, and `_query_combat` now applies a **defender-side** crusade block to the target. (3) `crusadeTraits.js` rebuilt: full by-type battle traits (Infantry/Mounted/Vehicle), generic scar table, generic `CRUSADE_RELICS`, and a standard `AGENDAS` reference — every effect mapped to a real flag where one exists. HonourPicker exposes the new groups. (4) 7 new math tests (76/76 green); 16/16 crusade-logic green; frontend builds. | `math_adapter.py`, `combat_math_engine.py` (n/a), `engine.py`, `crusadeTraits.js`, `HonourPicker.jsx`, `tests/test_combat_math.py` |
| ✅ DONE 2026-06-04 | S5 | **Campaign stats dashboard + agenda reference** — `CampaignStats` panel in `CrusadeContext` (between battles): win rate, total XP, total enemy kills, honours/scars counts, net crusade points, a supply-used bar (OOB points vs supply limit), and Top Killers / Most Experienced leaderboards. PostBattleFlow gained a collapsible standard-agendas reference beside the per-unit XP override (minimal agenda scope per build decision). | `CrusadeContext.jsx`, `crusade/PostBattleFlow.jsx` |

### Bugs

| Status | Priority | Description | Fixed In |
|--------|----------|-------------|----------|
| ✅ FIXED 2026-06-12 | **HIGH** | **Roster import silently dropped duplicate-named units** — `importUnits`/`_importLoop` (`lib/crusade.js`) deduped parsed units against a name\|nickname `Set` and added every imported unit's key to it, so a second identical datasheet within the *same* roster (very common: 2× Broadside, 3× Kroot Carnivores, etc.) was treated as a duplicate and skipped. An 18-unit / 1490pt T'au OOB imported as 13 units / 1095pt (the 5 repeat copies = exactly 395pt dropped). Fix: dedup is now **count-based** — a parsed unit is skipped only while a matching *pre-existing* OOB unit remains unconsumed; identical units within one roster no longer skip each other. Re-importing the same roster tops up to the correct count without duplicating. `ImportFlow` preview made count-aware to match (shared `_unitKey` helper). Verified against the reported roster: fresh import 18/1490, re-import into the broken 13-unit OOB adds exactly the 5 missing units. | `src/lib/crusade.js` `importUnits`/`_importLoop`/`_unitKey`, `src/components/crusade/OrderOfBattle.jsx` `ImportFlow` |
| ✅ FIXED 2026-06-12 | **HIGH** | **Crusade page 403'd — `CrusadeCampaigns`/`CrusadeUnits`/`CrusadeBattles` tables never existed in the base** — same root cause as the Reports table (line 56): the prod PAT lacks `schema.bases:write`, so `crusade_store._ensure_schema` silently no-ops and the tables were never auto-created; every read 403'd `INVALID_PERMISSIONS_OR_MODEL_NOT_FOUND`. Fix: created all three tables by hand in base `appfOqXB5mLMgXFKp` matching `TABLE_SPECS` exactly (timestamps as Single line text per the Reports date-typing lesson) — `CrusadeCampaigns` `tbl9KOVaMwOOHm2c5`, `CrusadeUnits` `tblw3GCnAlcXiJUcv`, `CrusadeBattles` `tbll32vRll4n1Ywdf`. No code change; the prod token already has data read/write on the base. | Airtable base `appfOqXB5mLMgXFKp` (schema only) |
| ✅ FIXED 2026-06-05 | **HIGH (P1)** | **`finalize_battle` claimed atomicity but wasn't** — 30+ sequential blocking Airtable round-trips with no rollback; a mid-loop failure left units half-updated and a retry double-applied XP / battle counts. True atomicity isn't possible over Airtable's REST API, so it was made **idempotent + resumable** via a `battle_token` (frontend mints a stable token per battle in `buildActiveBattle`, sent by `PostBattleFlow`). A token already in `state._finalized_battles` is a no-op; each unit's id is recorded in campaign `State` as it's applied (persisted in the loop's `except` AND once after the loop completes, before the battle-row write + campaign bump — closing the post-loop double-apply window a review caught); the battle row is keyed by the token so a retry can't duplicate it; the campaign bump reads a fresh snapshot. Three new fake-Airtable tests cover full-retry idempotency, mid-loop-failure resume, and post-loop-failure resume (each asserts XP applies exactly once). | `crusade_store.py` `finalize_battle`, `create_battle`; `server.py`, `src/lib/crusade.js`, `src/components/crusade/PostBattleFlow.jsx`, `tests/test_crusade_logic.py` |
| ✅ FIXED 2026-06-04 | **CRITICAL** | **Prod 502 Bad Gateway since the Session 1 deploy** — `Dockerfile` copies backend Python by an explicit allowlist (`server.py`, `profiles.py`, `shared_rosters.py`) but never `crusade_store.py`. Session 1 added `from render.web import crusade_store` at the top of `server.py`, so in the container that import raised `ImportError` at boot → uvicorn never started → Caddy returned 502. Reproduced in an image-like layout (no `__init__.py`; namespace packages) both ways: fails without the file, imports cleanly with it. Fix: added `COPY render/web/crusade_store.py render/web/crusade_store.py`. Lesson: this Dockerfile must be updated whenever a new `render/web/*.py` module is imported by `server.py`. | `Dockerfile` |

### Gaps / Deferred

| Priority | Gap | Notes |
|----------|-----|-------|
| LOW | `createUnit` body has no `attached_to` field | Import patches attachment via a second `updateUnit` call (extra round-trip). Could add `attached_to` to `UnitCreateBody` + `create_unit`. |
| LOW | Bulk import is N sequential POSTs | One create per parsed unit. Fine for OOB sizes; a `/units/bulk` endpoint would cut round-trips for large rosters. |
| MED | Muster shares the engine roster slot with VFS rosters | `startBattle` writes `_session["roster_my"]`; loading a VFS roster afterward (which sends its own `roster_context`) overwrites the mustered crusade roster. Fine for a single battle session; a dedicated crusade-battle slot would isolate them. |
| ✅ FIXED 2026-06-04 (S4) | Mark-for-Greatness now persisted + rewarded | Muster writes `marked` into `state.active_battle.units`; PostBattleFlow awards the +1 XP (§3.2) on finalize. (The CrusadeCard `marked_for_greatness` flag is also cleared on every unit at battle end.) |
| ✅ FIXED 2026-06-04 (S5) | Defender-side crusade applied | `_query_combat` now resolves a defender crusade block (searched in `roster_my` first — the player's crusade units live there regardless of which side of `vs` they sit on — then `roster_enemy`) and applies its DEFENSIVE flags to the target profile. `_crusade_combat_mods(entry, side)` classifies each honour/scar flag via `math_adapter.is_defensive_flag`: attacker side contributes offensive flags only, defender side defensive flags only. Also fixed a latent bug where an attacker's defensive honour (5+ FNP / invuln) leaked onto the target and wrongly buffed the defender. |
| ✅ FIXED 2026-06-04 (S5) | Stat-modifier traits/scars now wired into the math | New defensive math flags added to `math_adapter._apply_flags` + `KNOWN_FLAG_BASES`: `woundsplus[N]` (+/-N to target Wounds, floored at 1 → Reinforced Hull / Hardened / Weakened scar), `dmgreduce`/`dmgred` (−N damage suffered → Armour Plating), `svplus`/`svminus` (better/worse save → Hexagrammic Wards / Shell Shocked scar). `crusadeTraits.js` updated so these traits/scars/relics carry real flags. Remaining flag:null effects (Move, Ld, OC, Advance/Charge, Overwatch) genuinely have no combat-damage equivalent and stay display-only. Legend + flag-note callouts + 7 new math tests added (76/76 green). |
| LOW (scope) | **Scar rolls are manual selection, not auto D6** | Per the S4 build decision, destroyed units get a manual scar-table dropdown (incl. "no scar") instead of the spec §3.3 auto D6 (1–3 none / 4–5 scar / 6 devastating). `compute_auto_xp` is the tested logic; no D6 roller shipped. Revisit if an auto-roll-with-override is wanted. |
| ✅ ADDRESSED 2026-06-04 (S5, minimal) | Agenda XP — standard agendas documented + manual field | Per the S5 build decision (minimal scope): a curated `AGENDAS` reference table (name / XP / how-to) lives in `crusadeTraits.js` and is surfaced as a collapsible reference in PostBattleFlow next to the per-unit "XP gained" override. Agenda XP is entered manually into that field (`autoXp`/`compute_auto_xp` still accept the `agenda` passthrough). Full auto-tracked per-battle agenda objectives remain deferred — not planned unless requested. |
| LOW | RP actions are client-composed, not a backend transaction | Each §3.4 action runs its unit/campaign mutation then a separate `updateCampaign({rp})` from the browser (consistent with the OOB editor). A partial failure could decrement RP without applying the effect, or vice-versa. A backend `spend_rp` endpoint would make it atomic like `finalize_battle`. |
| LOW | Re-muster relies on the in-memory engine roster | `state.active_battle` durably restores the tracker UI, but the engine's `_session["roster_my"]` is in-memory; if the engine restarts mid-battle the BattleTracker "↻ Re-muster" re-pushes the roster from stored units. |

---

## Reports (render/web — `report` / `reports`)

User-filed problem reports. Any logged-in user can flag a missing unit / wrong
stat / bug; only the data owner can fix the dossiers, so reports collect
server-side (Airtable `Reports` table, self-provisioning like the Crusade tables)
for the owner to work through later.

### Done

| Status | Description | Component |
|--------|-------------|-----------|
| ✅ DONE 2026-06-05 | **Reports store + API** — `render/web/reports.py` (Airtable, mirrors `shared_rosters`/`crusade_store`: create/list/resolve/delete, `_ensure_schema` self-provisions the `Reports` table). Routes: `GET /api/reports?status=open\|resolved\|all`, `POST /api/reports`, `POST /api/reports/{id}/resolve`, `DELETE /api/reports/{id}` (Airtable failures → 503; empty body → 400; unknown id → 404). **Dockerfile updated** to `COPY render/web/reports.py` (the import-502 footgun from Session 1). Verified end-to-end via FastAPI TestClient with a mocked store + a fake-table unit test of the store logic. | `reports.py`, `server.py`, `Dockerfile` |
| ✅ DONE 2026-06-05 | **`report` / `reports` / `resolve` commands** — registered web-only (`supports_cli=False`) in `commands.py` (meta group). Handled client-side in `Terminal.jsx` (Priority 6.5): `report <text>` POSTs a report (auto-detects `missing unit X` / `missing weapon X` → category+subject; tags the reporter's profile name); `reports` lists open reports (`reports_list` renderer); `resolve <n>` closes the n-th report from the last list (mapped via `lastReportsRef`). New `lib/reports.js` API client. | `commands.py`, `Terminal.jsx`, `lib/reports.js`, `TerminalBlock.jsx` |
| ✅ DONE 2026-06-05 | **Error-message report + learn affordances** — `engine._err` now carries optional `report_command` / `learn_template` in `meta`. The combat "unit not found" error sets them; `TerminalError` renders a clickable **⚑ Report this to the admin** link (inject + submit) and an inline **`learn <typo> = <correct name>`** hint (fills the command bar without submitting, via the existing `onEdit` handler) for the case where the unit exists under another name. Frontend JS bundles clean (esbuild). | `engine.py`, `TerminalBlock.jsx` |

### Gaps / Deferred

| Priority | Gap | Notes |
|----------|-----|-------|
| LOW | Report affordance only on the combat not-found error | The clickable report/learn links could also be attached to the missing-leader combat warning and the per-weapon "fell out of the calculation" issue-log entries (both already carry the unit/weapon name). `_err` is generic, so it's a small follow-up. |
| LOW | Anyone can view/resolve reports | Per the build decision, no owner gating — fine for trusted friends. A designated-owner check would need a profile "owner" flag (none exists today). |
| LOW | No reports dashboard | Review is the `reports` command only (no REPORTS tab). Add a panel if the list grows. |

---

## Engine: _(next engine)_

> Copy this section template when adding a new engine.
> Fill in the determinism gaps immediately after building the stub commands.

### Bugs
_None yet_

### Determinism Gaps
_Document when commands are stubbed_

### Data Gaps
_Document when data exists but isn't wired_

### Architecture Improvements
_Document patterns that could be generalised_

---

## Cross-Engine / Framework Items

Things that would improve the engine framework itself, not any individual engine.

| Priority | Improvement | Notes |
|----------|-------------|-------|
| MED | `EngineBase` should include a `backlog()` method that returns this file's contents as structured data | Lets the renderer surface "what's a stub" to the user in the UI |
| MED | Standard `_stub` flag convention — every stub response should set `"_stub": true` so the renderer can display a visual indicator | Already done in combat_terminal; make it part of the contract |
| **MED** | **Verify auto-deploy agents on Phil-2** — launchd agents (`com.juzzie.war-autodeploy`) poll GitHub every 60s and rebuild on new commits. `/api/version` endpoint exists for verification. `.dockerignore` added 2026-06-02 to speed up builds (was sending 36MB node_modules into build context). Delete `Dockerfile.new` (duplicate of `Dockerfile`). To verify: push a commit, wait 90s, `curl -s https://war.juzzie.xyz/api/version`. If stale, check Phil-2: `launchctl list | grep juzzie` and `tail -20 ~/JuzzieSite/deploy/deploy-war.log`. | Infrastructure exists — needs verification on Phil-2, not repo-side CI/CD |
| LOW | Engine hot-reload — detect file changes in `data/<engine>/data/` and call `loader.load()` without restarting server | Useful during data development |
| LOW | `query("backlog")` as a built-in EngineBase command | Any engine can report its own gaps to the caller |
